# 하강 (가제) — 설계 문서

> **v1.2** · 갱신 2026-08-12 · 상태: 경주 전환 완료, 협동판 시스템 삭제됨

**20명이 지하 8층 미로를 먼저 빠져나가는 선착순 경주. 귀신을 피해 내려간다. 그게 전부다.**

최대 20인 경쟁 3D 1인칭. 한 변 57.5 m의 정사각 기둥 안에 동심 미로 여덟 층이
쌓여 있다. 전원이 B1 외곽 고리에서 동시에 출발하고, 각 층의 한가운데에 있는
**투하구**로 뛰어내리면 아래층의 **외곽**에 선다 — 그래서 미로를 여덟 번 푼다.
B8 중심에 **먼저 닿은 한 명**이 이긴다. 괴물은 죽일 수 없고, 잡히면 **B1의 자기
칸으로 되돌려진다** — 내려온 층을 전부 잃고, 그 외에는 아무것도 잃지 않는다.

**변경 이력**
- **v1.2 — 문서를 코드에 맞췄다 (전수 대조).** 두 가지가 바뀌었고 둘 다 *코드가 먼저
  바뀌어 있었다*: ① **잡히면 탈락이 아니라 B1로 되돌려진다** — §01 · §02 · §06 ·
  §11을 고치고 §09(유령)를 삭제 절로 바꿨다. 근거는 `RaceState.ReportCaught`와
  `RacerStatus.Eliminated`의 주석("Nothing in the game eliminates a player any more").
  ② **§12의 바닥 재질표가 「구역 A~D + 계단」 5칸에서 「여덟 층에 여덟 재질」로**
  바뀌었다. 근거는 `DescentMap.Storey`와 `FloorMaterial`, 그리고 96개의 발소리 클립.
- **v1.1 — DESCENT-PIVOT §7 step 7 실행. 상점 · 전리품 · 크레딧 · 단서 · 목표물 ·
  왕복 · 지상 · 배터리 경제를 문서와 코드에서 **삭제**했다. v1.0은 이것들을
  "게이트로 막아 둔" 상태였고, 그래서 계속 새어 나왔다.**
- v1.0 — 4인 비대칭 협동 회수 → 20인 경쟁 하강 경주. 근거는 [DESCENT-PIVOT.md](DESCENT-PIVOT.md)
- v0.5 — §13 텔레메트리를 DB 없는 3단계 전략으로 교체
- v0.4 — §05 조작과 이동 신설 (속도 배율 확정)
- v0.3 — 왕복 구조 · 시간 기반 위협 · 경제 시스템 · 맵 규칙
- v0.2 — 어그로 수치 · 상태 기계 · 진행도 위협 · 유령
- v0.1 — 초안

> **v1.1에서 무엇이 없어졌나 — 한 문단.**
> v1.0은 협동판 시스템을 *경주 경로에서 분기시켜* 두었을 뿐 지우지 않았다. 그 결과
> 첫 틱마다 상점 객체가 생성되고, 매 고정 스텝마다 전리품 상태가 동기화되고, 맵은
> 152개의 전리품 마커와 24개의 후보 지점을 계속 배치했다. **경주에서 쓸 수 없는
> 것을 반쯤 남겨 두면, 그것은 없어진 것이 아니라 숨은 것이다.** v1.1은 지웠다.
> 아래에서 삭제된 절은 번호를 그대로 두고 그 자리에 무엇이 있었고 왜 없앴는지를
> 적는다 — 코드가 §번호로 수치를 인용하기 때문에, 번호가 사라지면 상수가 미아가 된다.

---

## 목차

| § | 제목 |
|:--:|---|
| [01](#01-개요와-한-판의-흐름) | 개요와 한 판의 흐름 |
| [02](#02-승패-조건) | 승패 조건 |
| [03](#03-어둠과-시야--안쪽으로-갈수록-보이지-않는다) | 어둠과 시야 |
| [04](#04-직업--삭제됨-스무-명이-같은-몸이다) | 직업 — 삭제됨 |
| [05](#05-조작과-이동) | 조작과 이동 |
| [06](#06-괴물--위험-요소이지-적수가-아니다) | 괴물 — 위험 요소 |
| [07](#07-시간--위협도) | 시간 = 위협도 |
| [08](#08-경제--삭제됨) | 경제 — 삭제됨 |
| [09](#09-유령--삭제됨-아무도-탈락하지-않으므로-유령이-될-사람이-없다) | 유령 — 삭제됨 |
| [10](#10-딜레마-지도) | 딜레마 지도 |
| [11](#11-인원-구조--최대-20인) | 인원 구조 — 최대 20인 |
| [12](#12-맵-설계-규칙) | 맵 설계 규칙 |
| [13](#13-인프라와-기술-스택) | 인프라와 기술 스택 |
| [14](#14-개발-순서) | 개발 순서 |
| [15](#15-설계-이력) | 설계 이력 |
| [16](#16-미결-과제) | 미결 과제 |

---

## 01. 개요와 한 판의 흐름

최대 20인 경쟁 호러, **3D 1인칭**. 모두가 같은 것을 보고 같은 것을 듣고 같은 몸으로
출발한다. 비대칭은 **아무 데도 없다.** 차이를 만드는 것은 오직 **길을 아는가**와
**어디까지 감수하는가**다.

### 핵심 루프

```
B1 외곽 (최대 20명 동시 출발)
   │  미로를 뚫고 중심으로
   ▼
B1 중심 투하구  ──낙하──►  B2 외곽
   │
   ▼          … 여덟 층 반복 …
   ▼
B8 중심 = 도착점.  먼저 닿은 한 명이 이긴다.
```

**층마다 외곽에서 중심으로.** 투하구는 언제나 중심에 있고, 떨어지면 언제나 다음 층
*외곽*에 선다. 그래서 층을 내려갈 때마다 미로를 처음부터 다시 푼다 — 여덟 번.

### 착지가 외곽인 이유

투하구가 중심에 있는데 착지도 중심이면, 한 번 중심에 닿은 사람은 그대로 여덟 층을
떨어져 끝난다. 미로가 한 번뿐인 게임이 된다. 착지점을 외곽으로 보내야 **각 층이 독립된
미로**가 되고, 그때 비로소 "미로 탈출을 여덟 번"이라는 형태가 성립한다.

중심의 투하구는 층마다 **2~3개**다. 어느 것으로 뛰어내리느냐가 아래층 어디에서
시작하느냐를 정한다 — 무작위가 아니라 고정된 대응이므로, 맵을 아는 사람이 유리하다.
그것이 이 게임의 실력이다.

### 한 판의 흐름 (12~20분)

> 🔴 **코드는 이 숫자에 동의하지 않는다. 결정이 필요하다 (2026-08-12 실측).**
> `GameConstants.TargetMatchSecondsMin/Max` = **25분 / 35분**이다 — 협동판 왕복
> 구조의 숫자가 그대로 남았다. 이 문서는 12~20분이라고 말한다.
>
> **무해한 불일치가 아니다.** `ThreatTierSeconds` = 8분이고 §07은 다섯 단계다. 즉
> 위협 곡선이 끝까지 도는 데 **40분**이 걸린다. 12분짜리 판에서는 1.5단계밖에 돌지
> 않으므로 §07의 압박이 사실상 착지하지 않고, §02의 「시간 초과」는 도달 불가능한
> 결말이 된다. 그리고 `FoundationTests.ThreatCurve_FitsInsideAMatch`가
> `TargetMatchSecondsMin / ThreatTierSeconds >= 3`을 검사한다 — 25분에서는 3.125로
> 통과하고, **12분으로 내리면 1.5가 되어 코어 스위트가 빨개진다.**
>
> 그러므로 셋 중 하나다: ① §01을 25~35분으로 되돌린다, ② 상수를 12~20분으로 내리고
> `ThreatTierSeconds`를 8분에서 **약 3분**으로 함께 줄인다, ③ 단계 수를 줄인다.
> `ARCHITECTURE.md` §6에 따라 여기서 조용히 고르지 않는다 — 숫자를 적어 두고
> 설계자가 고른다. §14의 플레이테스트 질문 4번이 이것을 잰다.

```
출발     최대 20명이 B1 외곽 고리에 흩어져 선다. 밝고, 넓고, 안전하다.
        전원이 동시에 안쪽으로 움직인다.

초반     외곽→중간 관문 4개에 스무 명이 몰린다. 여기서 첫 순위가 갈린다.
        문을 닫고 가는 사람이 나온다.

중반     중간→안쪽 관문 2개. 괴물의 순찰 반경이 시작되는 곳.
        선두는 위험을 통과하고, 안전하게 간 사람은 뒤처진다.

후반     안쪽→중심 관문 1개. 그 층 전원이 같은 한 칸을 지난다.
        §07이 올라가 있어 늦은 사람일수록 괴물이 빠르다.

도착     B8 중심. 한 명.
```

### 왕복이 없다 — 이것이 v1.1의 가장 큰 삭제다

v0.5의 한 판은 **들어갔다 나왔다를 3~4회** 반복하는 구조였고, 그 위에 상점 · 재보급 ·
시계 확인이 전부 얹혀 있었다. 경주는 **한 번의 하강**이고 되돌아갈 곳이 없다.

> **지상(地上)은 없다.** 건물 밖으로 나가는 길이 없다. 예전의 「출입구」 마커는
> 그대로 §02의 **도착점**이 되어 B8 한가운데로 내려갔다. 나가는 문이 곧 이기는
> 자리이고, 그 자리는 스무 명이 뚫어야 할 여덟 겹 아래에 있다.

### 장르적 결정 세 가지

**살상 수단이 없다**
- 괴물을 죽일 수 없다
- 저지는 전부 일시적 (문)
- 이길 수 없는 적 → 공포가 유지된다

**플레이어끼리도 죽일 수 없다**
- 방해 수단은 **문**과 **괴물을 남에게 떠넘기는 것**뿐이다
- 직접 타격이 없으므로 스무 명이 서로를 때려눕히는 난투가 되지 않는다
- 경주의 무기는 폭력이 아니라 **지형**이다

**잡히면 처음부터다**
- 팀이 없으니 일으켜 줄 사람도 없다
- 잡힌 사람은 **B1의 자기 칸**으로 돌아가 계속 달린다 (§02 · §06)
- 그래서 협동판보다 괴물이 더 무섭다 — 죽여서가 아니라 **되감아서**

---

## 02. 승패 조건

| 결과 | 조건 |
|---|---|
| **승리** | B8 중심 도착점에 **가장 먼저** 닿는다. 한 명뿐이다 |
| **완주** | 도착점에 닿았으나 1등이 아니다 — **순위가 기록된다** |
| **되돌려짐** | 괴물에게 잡힌다. **B1의 자기 칸에서 다시 달린다.** 탈락이 아니다 |
| **시간 초과** | 아무도 도착하지 않고 §07이 마지막 단계를 지난다 — 전원 패배 |

> **핵심: 이기는 방법은 하나뿐이고, 지는 방법은 여러 가지다.**
>
> 1등이 아니어도 완주 순위는 남는다. 그래서 선두를 놓친 사람에게도 계속 달릴 이유가
> 있고, 마지막 한 층에서 3등이 2등을 문으로 막는 일이 벌어진다.
>
> **잡히면 시간을 잃지, 판을 잃지 않는다.** 괴물은 잡은 사람을 B1의 자기 칸으로
> 되돌린다. 내려온 층을 전부 잃으므로 대가는 크지만 — 그리고 §07이 그 사이에도 계속
> 올라가므로 두 번째 하강은 첫 번째보다 위험하다 — **경주에서 빠지지는 않는다.**
>
> 시간 초과가 존재하는 이유는 §07 때문이다. 전원이 안전하게만 움직이면 괴물이
> 5.2 m/s가 되고 아무도 안쪽 고리를 통과하지 못한다. **누군가는 위험을 감수해야
> 판이 끝난다.**

> 🔴 **v1.1은 「탈락」이었다. 코드가 먼저 바꿨고, 이 문서가 뒤늦게 따라왔다
> (2026-08-12 확인).** 예전 문장은 「탈락에는 순위가 없다 … 화면에도 숫자가 아니라
> **「탈락」** 두 글자만 뜬다」였다. 지금 출하되는 규칙은
> `Core/Race/RaceState.cs`의 `ReportCaught`이고, 그것은 `RacerStatus.Running`과
> 층 `0`을 쓰고 `TimesCaught`를 하나 올린다 — 상태를 바꾸지 않는다.
> `RacerStatus.Eliminated`의 주석이 그 자리에서 직접 말한다:
> 「**Nothing in the game eliminates a player any more**; this is what a disconnect
> resolves to.」 즉 `Eliminated`는 이제 **접속이 끊긴 자리**를 뜻하고, `Over`가
> 언젠가 참이 되게 하려고 남아 있다.
>
> **왜 바꾸는 쪽이 옳았는가.** 20인 경주에서 접촉 한 번에 탈락시키면 초반 3분에
> 절반이 관전자가 되고, 그 사람들에게 남는 것은 §09의 흔들기 하나뿐이다. 되돌리면
> 벌은 더 무겁게 느껴지는데(§07이 올라가 있다) 게임은 계속된다.

### 순위 판정은 호스트가 한다

§13의 호스트 권한 원칙 그대로다. 도착 판정을 클라이언트가 내리면 경주 게임에서
가장 먼저 조작되는 값이 된다. `RaceState`가 순위를 계산하고, HUD는 그것을 *읽기만*
한다 — 화면 쪽에는 "도착했다"고 말할 수 있는 메서드 자체가 없어야 한다.

> **삭제됨 — 협동판의 네 결말.** v0.5의 §02는 완전 승리 / 부분 승리 / 생존 / 패배
> 넷이었고, 「한 명이라도 탈출하면 정보가 보존된다」·「전멸하면 전리품·크레딧·정보가
> 전부 사라진다」로 팀 단위 정산을 했다. 팀이 없고 통화도 없으니 정산할 것이 없다.
> 위의 네 줄이 그 자리를 대신하며, 이쪽은 **한 사람의 결말**이다.

---

## 03. 어둠과 시야 — 안쪽으로 갈수록 보이지 않는다

> **어둠은 남고, 어둠의 관리는 없앴다.** v0.5의 §03은 「목표 구조 — 3층위와 왕복」
> 이었다. 단서로 목표물 위치를 좁히는 구조는 **목적지가 처음부터 알려진 경주에서
> 성립하지 않아** 통째로 걷어냈고, v1.0에서 남겨 두었던 배터리도 v1.1에서 없앴다.

### 고리별 조명

| 고리 | 조명 | 손전등 | 의미 |
|---|---|---|---|
| **외곽** | 상시 켜짐 | 필요 없음 | 스무 명이 서로를 본다. 출발선 |
| **중간** | 꺼져 있음 | 필요 | 여기서부터 빛은 내가 들고 온 것뿐이다 |
| **안쪽** | 없음 | 필수 | 손전등 사거리 바깥은 존재하지 않는다 |
| **중심** | 투하구만 빛난다 | — | 목적지는 멀리서도 보인다 |

**목적지가 보인다는 것이 중요하다.** 중심의 투하구는 안쪽 고리 어디서든 보이는
빛이다. 길을 잃는 것이 아니라 **가는 길이 막혀 있는 것**이 이 미로의 어려움이다.
보이는데 못 가는 것이 안 보여서 못 가는 것보다 훨씬 분하다.

### 손전등은 그냥 켜진다

**배터리·배전반·발전기를 전부 삭제했다.** 판당 셀 개수, 잔량 게이지, 교체를 위한
왕복, 구역을 밝히기 위한 전기 패널 — 전부 없다. 손전등은 켜고 끄는 스위치 하나다.

> **왜 없앴는가.** 완전한 암전은 플레이가 불가능하고, 손전등을 관리하는 일은
> 오너가 지적한 그 잡일 그 자체다 — 「자꾸 단서를 찾고 불을 밝히고 이러고있어」.
> **유지해야 하는 빛은 심부름이고, 들고 다니는 빛은 게임이다.** 12~20분짜리 경주에서
> 자원 관리 축을 하나 더 얹으면 경주가 흐려질 뿐 어려워지지 않는다.
>
> 대신 손전등은 **끄는 쪽으로 비용이 남는다** — 켜면 괴물이 본다(§06), 끄면 §10의
> 그늘이 고인다. 스위치 양쪽에 값이 붙어 있는 한 딜레마는 유지되고, 그 값은 배터리
> 잔량이 아니라 **보이느냐 / 무엇을 잃느냐**다.

### 밝기는 설정에서 조절할 수 없다 — 어둠은 규칙이다

밝기 슬라이더의 폭은 **±20 %**로 고정한다. 두 배로 밝힐 수 있으면 안쪽 고리를
손전등 없이 읽을 수 있고, 절반으로 어둡게 할 수 있으면 15 m에서 보이도록 만들어 둔
괴물의 실루엣이 사라진다. 이 0.20이라는 수치는 코드에서
**`GameConstants.MinSafeLightQuality` = 0.20f** 이고, §03의 「빛이 있다 / 없다」
경계다. §10의 그늘도 같은 경계를 쓴다 — `PresenceDensity.SafeLightQuality`가 정확히
이 값이다.

> ✅ **개명 완료 (2026-08-12 확인).** 이 자리에는 「이 상수는 `MinLitLightQuality`로
> 개명하고 … 삭제된 시스템을 인용하는 유일한 남은 수치다 (§16-3)」가 있었다. 실제
> 개명은 `MinSafeLightQuality`로 됐고 — 「읽을 만큼 밝다」가 아니라 「그늘이 생기지
> 않을 만큼 밝다」가 살아남은 쪽이므로 그편이 더 정확하다 — 값은 0.20 그대로다.
> §16의 3번 줄도 닫힌 것으로 읽을 것.

---

## 04. 직업 — 삭제됨. 스무 명이 같은 몸이다

> **v1.0에서 다섯 직업을 전부 걷어냈다.** 청음사 · 관측자 · 주자 · 정비공 · 섬광수는
> 4인이 서로를 메우는 구조였고, 20인 경주에는 메울 팀이 없다.

**스무 명이 완전히 같은 몸으로 출발한다.** 같은 속도, 같은 손전등, 같은 손, 같은 귀.

### 왜 능력을 나눠 주지 않는가

능력을 나눠 주면 경주의 결과가 **픽으로 설명된다.** 진 사람은 자기가 진 이유를
자기 선택이 아니라 자기 픽에서 찾고, 그러면 다음 판에 고칠 것이 없다. 스무 명이 같은
출발선에 서면 1등과 20등 사이의 차이는 전부 그 판에서 한 행동이다.

또 하나. 픽이 있으면 **최적 픽이 생긴다.** 스무 명 중 열여덟이 같은 것을 고르는 순간
선택은 사라지고 밸런싱만 남는다. 없애는 것이 균형을 맞추는 것보다 확실하다.

### 전원이 가진 것 — 네 가지

| | |
|---|---|
| **손전등** | 켜고 끈다. 관리할 것이 없다 (§03) |
| **문** | 누구나 닫고 연다. 닫는 데 1.1초 · 부서지면 안 돌아온다 (§12-B) |
| **질주** | 5.6 m/s, **체력 제한.** 아래 |
| **귀** | 발소리는 누구에게나 들린다. 바닥 재질에 따라 다르게 (§12) |

넷 중 **손에 드는 것은 손전등 하나뿐이다.** 들어서 시야를 막는 물건도, 두 손을 쓰는
물건도, 무게로 느려지는 물건도 없다 — 전부 §08과 함께 삭제했다.

### 질주는 남는다 — 전원에게, 체력으로

v0.5에서 5.6 m/s는 주자 전용이었다. 직업이 없어졌으니 **전원에게 준다.**
대신 무한하지 않다: 스태미나 12초가 있고, 다 쓰면 달리기 속도(4.5)로 떨어지고,
서 있으면 20초에 회복한다.

이 숫자가 남아야 하는 이유는 §06에 있다. 괴물의 덮치기는 **달리는 사람(4.5)은 잡고
질주하는 사람(5.6)은 놓치도록** 맞춰져 있다. 질주를 없애면 괴물에게 걸린 순간
누구도 살 수 없고, 그러면 §12의 시야 차단 지형이 통째로 무의미해진다.

질주가 유한하다는 것이 경주의 핵심 결정을 만든다:

> **지금 쓸까, 관문에서 쓸까.**
>
> 초반에 태우면 앞서지만 마지막 관문 하나를 스무 명과 함께 통과할 때 아무것도 없다.
> 아껴 두면 그 관문에 늦게 도착한다.

---

## 05. 조작과 이동

### 기본 조작 — 표준 FPS

| 입력 | 동작 |
|---|---|
| `마우스` | 시점 회전 (yaw + pitch) — **이것이 "앞"을 정의한다** |
| `W` / `S` | 시점 기준 전진 / 후진 |
| `A` / `D` | 좌우 스트레이프 |
| `Shift` | 달리기 · 계속 누르면 질주 (스태미나 소모) |
| `Ctrl` | 웅크리기 — 느리고 조용하다 |
| `Space` | 뛰기 — 착지가 시끄럽다 |
| `E` | 상호작용 — **문뿐이다** |
| `F` | 손전등 on/off |

**마우스 방향 = 이동 방향.** 뒤를 보려고 마우스를 돌리면 이동 기준도 함께 돌아간다.

> `E`가 가리키는 대상이 문 하나로 줄어든 것은 v1.1의 삭제 결과다. 단서 · 전리품 ·
> 목표물 · 금고 · 차량이 전부 없어졌고, 조준선이 찾는 상호작용 대상은 §12-B의 문뿐이다.
> **키 자체는 지우지 않았다** — 경주에서 문은 남을 막는 유일한 수단이고, 조준선에서
> 키까지의 경로는 이 저장소가 한 번 통째로 고장난 채 출시 직전까지 간 적이 있는 곳이다.

### 속도 배율표 ← 추격의 문법

| 입력 | 방향 | 배율 | 질주 시 | 괴물(4.8) 대비 |
|:--:|---|:--:|:--:|---|
| `W` | 전진 | 100% | **5.6** | **+0.8** 벌어짐 |
| `W+A/D` | 대각 | 95% | 5.32 | +0.52 벌어짐 |
| `A/D` | 측면 | 90% | 5.04 | +0.24 벌어짐 |
| **`S`** | **후진** | **65%** | **3.64** | **−1.16 좁혀짐** |

> **핵심 결론: 뒷걸음은 괴물보다 느리다. 뒤를 보면 잡힌다.**
>
> 추격 중 거리를 확인하려면 뒤를 봐야 하고, 뒤를 보면 느려진다.
> §10 원리의 가장 직접적인 구현이며, 페널티가 애매한 서술이 아니라
> **거리 손실이라는 수치**로 확정된다.

### 45도 곁눈질 = 숙련 기술

```
정면 도주       속도 100%,  괴물 안 보임
45도 곁눈질     속도  95%,  괴물이 시야 끝에 걸림   ← 숙련
90도 측면       속도  90%,  괴물이 거의 정면
완전히 뒤       속도  65%,  잡힌다
```

플레이어가 **정보량과 속도를 연속적으로 조절**할 수 있다.
이산적 선택이 아니라 **아날로그 조절**이라는 점이 중요하다 — 마우스를 몇 도 돌릴지가 곧 실력이다.

| 숙련도 | 플레이 |
|---|---|
| 초보 | 앞만 보다 해제 타이밍을 놓치거나, 뒤를 봐서 잡힌다 |
| **숙련** | **45도로 곁눈질하며 거리를 관리하고** 정확한 순간에 시야를 끊는다 |

### FOV — 밸런스 항목이다

| FOV | 효과 |
|:--:|---|
| 60~70 | 호러 정석. 답답함 최대. **곁눈질이 거의 안 됨** |
| **75~85** | ✅ 곁눈질이 성립하면서 시야는 여전히 좁다 |
| 95+ | 곁눈질이 너무 쉬워 딜레마가 약화된다 |

**기본 80, 조절 범위 70~90.** 고정하면 멀미 민원이 발생하고,
무제한으로 열면 넓게 설정한 쪽이 유리해진다. 범위 제한이 절충점이다.
설정 파일을 손으로 고쳐도 **읽는 쪽에서 잘라낸다** — 클램프는 슬라이더가 아니라
로더에 있어야 한다.

### 손전등이 포인터가 된다

> 근접 음성(§13)과 결합하면 *"저기! 저기 봐!"* + 손전등으로 비추는 동작이
> **말과 빛으로 동시에 위치를 전달**한다.
>
> 경주에서 이것이 왜 남는가: 정보를 나눠 주는 것이 **손해가 아닌 경우가 있다.**
> 뒤에서 오는 사람에게 괴물이 있는 쪽을 비춰 보이면, 그 사람은 그쪽을 피해 가고
> 괴물은 계속 나를 쫓는다. 거짓말도 할 수 있다. **경주의 대화는 협동이 아니라 협상이다.**
>
> 따라서 **카메라 회전을 네트워크로 동기화해야 한다** — 남의 손전등 방향이 정보다.

### 조작 규칙 결정

| 항목 | 결정 | 이유 |
|---|---|---|
| 문을 닫는 동안 | **이동만 정지**, 마우스룩 허용 | 화면이 얼면 조작감 최악. 발이 묶인 채 둘러보는 게 더 무섭다 |
| 달리며 뒤 보기 | **자유롭게 허용** | 속도 손실이 페널티. 벽 충돌은 콘텐츠 |
| 소리의 방향 판별 | **몸을 돌려 삼각측량** | 3D 오디오는 카메라 기준 → **헤드폰 필수** |
| 시점 흔들림 | **0까지 끌 수 있다** | 유일하게 밸런스에 닿지 않는 설정 행. 멀미로 게임을 못 하게 두지 않는다 |

> **삭제된 행:** 「목표물 운반 중 — 마우스룩 가능, 손전등 불가」. 운반할 것이 없다.

### 네트워크 동기화 항목

| 항목 | 이유 |
|---|---|
| 위치 | 기본 |
| **카메라 회전 (yaw + pitch)** | **손전등 방향 = 정보.** pitch까지 필요 — 바닥·천장을 비추는 것도 신호 |
| 손전등 on/off | 괴물 인지에 영향 |
| 스태미나 | 본인만 정확히, 남은 대략 |
| 층 · 순위 | §02의 판정은 호스트만 한다. 클라이언트는 결과를 받는다 |

> **에셋 비용**
>
> **1인칭이어도 캐릭터 모델이 필요하다** — 앞에 사람이 있다는 사실이 경주의 정보다.
> 필요한 것은 **플레이어 모델 1종**(§04에 직업이 없으므로 직업 색도 없다) +
> **애니메이션 3종**(서기 / 걷기 / 달리기) + 손전등 부착.
>
> 오히려 이쪽이 낫다. **똑같이 생긴 스무 명이 어두운 복도에서 같은 방향으로 움직이는 것**이
> 각자 다르게 생긴 스무 명보다 무섭다. 누가 사람이고 누가 아닌지도 한순간 헷갈린다 —
> §10의 그늘이 노리는 것이 그것이다.

### §06 · §12 수치에 미치는 영향

```
기존 계산:  전진 100% 가정 → 초당 0.8m 벌어짐 → 12초에 9.6m
실제:      곁눈질 95%     → 초당 0.52m       → 12초에 6.2m
```

| 항목 | 기존 | 조정 |
|---|:--:|:--:|
| 질주 최대 이동 거리 | 67m | **60m** |
| 시야 차단 지점 간격 | 15~25m | 유지 (3~4회 기회) |

**어그로 해제가 계산보다 어려워진다.** 거리를 확인하면서 도망치면 덜 벌어지기 때문이다.
최적 플레이가 어려워야 실력 여지가 생기므로 이는 바람직한 방향이다.

---

## 06. 괴물 — 위험 요소이지 적수가 아니다

> **이 절의 성격이 v1.1에서 바뀌었다.** 협동판에서 괴물은 **적**이었다 — 팀이 그것을
> 피해 목표를 회수하는 것이 게임이었다. 경주에서 괴물은 **적수가 아니라 지형의
> 일부**다. 이기는 상대는 다른 열아홉 명이고, 괴물은 그들 사이를 갈라놓는 **위험
> 요소**다. 잡히면 지는 게 아니라 **처음으로 되감긴다** — B1의 자기 칸이다(§02).
>
> 왜 이 구분이 중요한가: 괴물이 적이면 플레이어는 괴물을 **상대하려** 든다. 위험
> 요소면 **지나가려** 든다. 이 게임이 원하는 것은 후자다. 그래서 괴물에게는 체력도,
> 약점도, 패턴 공략도 주지 않는다. **날씨처럼 다뤄야 한다.**

### 속도 관계 — 이 한 줄이 게임 전체를 정한다

```
걷기 2.0  <  달리기 4.5  <  괴물 4.8  <  질주 5.6   (m/s)
```

| 결과 | |
|---|---|
| 달리기로는 **도망칠 수 없다** | 시야를 끊거나 문을 닫아야 한다 (§12) |
| **질주만 도망칠 수 있다** | 유한한 스태미나가 경주의 핵심 결정이 된다 (§04) |
| 스태미나가 끝나면 잡힌다 | 무한 도주 방지 |

괴물이 달리기보다 **0.3만 빠른** 것이 핵심이다.
아주 조금 빠르면 **"거의 도망칠 수 있는데 안 되는"** 최악의 긴장이 생긴다.

> 위 값은 전진(100%) 기준이며 §05의 배율이 곱해진다. 괴물 속도는 시간대별로 변동한다(§07).

### 어그로 해제 — 거리만으로는 불가능하다

```
질주 5.6 − 괴물 4.8 = 초당 0.8m 벌어짐
스태미나 12초 × 0.8      = 최대 9.6m
해제 거리 12m            → 질주만으로는 절대 못 벌린다
```

> **어그로 해제는 거리가 아니라 맵을 쓰는 것이다.**
> 모퉁이를 돌고, 문을 닫고, 구조물을 끼고 돌아야 한다. §12 맵 설계 규칙의 근거.

| 파라미터 | 값 | 비고 |
|---|:--:|---|
| 해제 거리 | `12 m` | |
| 시야 차단 유지 | `3 초` | 단일 모퉁이로는 부족(§12) |
| 스태미나 (질주 지속) | `12 초` | 최대 이동 60m (§05 반영) |
| 스태미나 완전 회복 | `20 초` | |
| **해제 후 괴물 행동** | — | **마지막 목격 위치로 이동 → 수색** |

> **경주에서 가장 중요한 한 줄:** 어그로가 풀리면 괴물은 **마지막으로 본 위치**로 향한다.
> 따라서 남들이 있는 쪽에서 어그로를 끊으면 **괴물을 그들에게 배달하는 것**이 된다.
> 협동판에서 이것은 사고였다. 경주에서는 **전술**이다.
>
> 도망치는 *방향*이 곧 전략이 되는 지점이며, 거리 기반 해제 설계가 경주에서
> 뜻밖에 잘 맞는 이유이기도 하다. 규칙을 하나도 바꾸지 않았는데 의미가 바뀌었다.

#### 진짜 딜레마 — 질주를 언제 쓸 것인가

- 처음부터 질주 → 거리는 벌지만 차단 지점 도달 전에 소진
- 아껴두면 → 그 사이에 잡힐 수 있음

**맵을 알아야 최적화된다.** 실력이 개입하는 지점이고, 그래서 반복해도 재밌다.

### 상태 기계

| 상태 | 행동 | 소리 | 전이 조건 |
|---|---|:--:|---|
| **순찰** | 경로 이동 | 발소리 | 소리 감지 → 경계 |
| **경계** | 소리 방향으로 이동 | 발소리 | 시야 확보 → 추격 / 3초 무소득 → 순찰 |
| **추격** | 직진 | 발소리+포효 | 해제 조건 충족 → 수색 |
| **수색** | 마지막 위치 반경을 뒤짐 | 발소리 | 15초 무소득 → 순찰 |
| **정지** | 멈춰서 듣는다 | **없음** | 소리 감지 → 경계 / 5초 후 → 순찰 |

> **정지 상태가 이 게임의 무기**
>
> 괴물이 멈추면 **소리를 내지 않는다.** 그러면 **위치를 잃고**,
> 플레이어는 *"어디 갔어? 방금 여기 있었는데"* 하게 되고, 방심하고 나오는 순간 걸린다.
>
> 순찰 중 랜덤하게(15~30초 간격) 정지를 넣는다. **침묵이 가장 무서운 소리다.**

### 잡히면 — B1로 되돌려진다

| | |
|---|---|
| 판정 | 접촉. 체력도 부상도 없다. 한 번이면 잡힌다 |
| 결과 | **B1의 자기 칸에서 다시 시작.** 내려온 층을 전부 잃는다 |
| 잃지 않는 것 | 순위 자격, 남은 시간, 스태미나 — 그리고 **경주에 남아 있을 권리** |
| 기록 | `RaceState`가 `TimesCaught`를 센다 |
| 판은? | **계속된다.** 스무 명 중 누구의 경주도 끝나지 않았다 |

**대가는 거리이지 참가 자격이 아니다.** B5에서 잡히면 네 층을 다시 풀어야 하고,
그동안 §07은 계속 올라가 있으므로 두 번째 하강의 괴물은 첫 번째보다 빠르다. 그것이
벌이다 — 앞선 사람일수록 잃을 것이 많다는 점에서 자동 보정이기도 하다.

> 🔴 **이 절은 「탈락. 순위 없음. §09의 유령」이었다 (2026-08-12 정정).**
> `Core/Race/RaceState.ReportCaught`는 상태를 `Running`으로 유지하고 층을 `0`으로
> 되돌린다. `RacerStatus.Eliminated`는 이제 접속이 끊긴 자리만 뜻한다.
> 「판은 계속된다」는 줄만 그대로 옳았다 — 이유가 「열아홉 명이 남아서」에서
> 「아무도 빠지지 않아서」로 더 강해졌을 뿐이다.

---

## 07. 시간 = 위협도

> **시간이 유일한 통화다.** — 이 문장은 v0.5에서 왔고, v1.1에서 **문자 그대로 참이
> 되었다.** 예전에는 크레딧이라는 두 번째 통화가 있었다. 지금은 없다.

| 행동 | 비용 |
|---|:--:|
| 막힌 길로 들어갔다 나오기 | ~20초 |
| 관문 앞에서 사람이 빠지길 기다리기 | ~10초 |
| 문을 닫고 가기 | 1.1초 (그리고 뒤에게 4.5초) |
| 안전한 우회로 | ~40초 |
| 괴물을 한 번 떼어내기 | ~30초 |

### 시간대별 위협 단계

| 경과 | 시각 | 속도 | 순찰 | 정지 | 추가 |
|:--:|---|:--:|---|:--:|---|
| 0~8분 | 초저녁 | 4.4 | 1개 구역 | 잦음 | — |
| 8~16분 | 밤 | 4.6 | 2개 구역 | 보통 | — |
| 16~24분 | 심야 | 4.8 | 절반 | 드묾 | **손전등 반경 −30%** |
| 24~32분 | 새벽 | 5.0 | 전체 | 없음 | **괴물이 투하구를 안다** |
| 32분+ | 동트기 전 | 5.2 | 전체 | 없음 | 생존 불가 수준 |

**이 표가 경주에서 하는 일은 지각자 처벌이다.** 늦게 가는 사람일수록 빠른 괴물을
만난다. 선두가 위험을 통과해 얻은 이득은 **후미가 더 위험한 판을 만난다**는 형태로
복리가 된다. 그리고 §02의 시간 초과가 표 맨 아래에 있어서, 전원이 안전만 택하면
아무도 이기지 못한다.

### 시계는 하나뿐이고, 그것은 경주 시계다

**시각을 확인하러 나갈 곳이 없다** — 지상이 없기 때문이다(§01). 대신 HUD에는
출발부터 흐른 시간이 있고, 그게 전부다. 회중시계도, 구매도, 「지상에서만 알 수 있다」는
제약도 삭제했다.

> **HUD가 절대 보여 주지 않아야 하는 것:** 괴물의 정확한 현재 속도, 다음 단계까지 남은
> 초. 경과 시간과 단계 이름까지다. 초 단위를 읽을 수 있으면 §07의 표는 압박이 아니라
> 시간표가 된다.

### 왜 잠입 횟수 기반이 아니라 시간 기반인가

*(v0.3의 논증. 왕복이 없어진 지금도 결론은 같아서 남긴다.)*

| 항목 | 잠입 횟수 기반 *(폐기)* | 시간 기반 *(채택)* |
|---|---|---|
| 쉬는 것 | 공짜 | **대가가 있다** |
| 압박 | 이산적 | **연속적** |
| 구현 | 횟수 카운터 + 페널티 | **시계 하나** |

**시스템은 줄었는데 긴장은 늘었다.** 좋은 신호다.

---

## 08. 경제 — 삭제됨

> **v1.0에서 대부분을, v1.1에서 나머지를 걷어냈다.** 이 자리에는 상점 · 크레딧 ·
> 전리품 4종 · 무게 페널티 밴드 · 가방 · 판매 · 지상 재보급 · 차량 · 상점 UI가
> 있었다. 전부 **왕복 구조 위에 세워진 것**이었고, 경주에는 왕복이 없다.
>
> v1.0은 배터리 하나를 §03으로 옮겨 살려 두었다. **v1.1은 그것도 지웠다** — 자세한
> 이유는 §03의 「손전등은 그냥 켜진다」에 있다. 요약하면: 관리해야 하는 빛은 오너가
> 없애 달라고 한 그 잡일이다.
>
> **삭제된 코드:** `Core/Economy/` 전체(Shop · Wallet · Inventory · LootId ·
> LootCatalogue · CarryLoad · LootSafe · SharedLootCarry), `Core/Light/BatteryState`,
> 전리품/금고/차량 상호작용, 상점 화면과 그 보드, HUD의 적재·배터리 게이지.
>
> 원문은 git 이력에 남아 있다 (`docs/game-design.md` v0.5 §08). 언젠가 협동 모드를
> 별도로 만든다면 그때 되살릴 것이지 지금 반쯤 남겨 둘 것은 아니다. **v1.0이 반쯤
> 남겨 두었고, 그것이 이번 라운드가 존재하는 이유다.**

---

## 09. 유령 — 삭제됨. 아무도 탈락하지 않으므로 유령이 될 사람이 없다

> **v1.1에서 이 절은 「탈락 처리 — 유령」이었다. 2026-08-12에 코드에 맞춰 지웠다.**
> 잡힌 사람은 B1로 되돌려져 계속 달린다(§02 · §06). 탈락이 없으면 관전자가 없고,
> 관전자가 없으면 유령 상태가 붙을 자리가 없다.
>
> **여기에 있던 것:** 맵 전체 투시 · 말하기 불가 · 45초 쿨타임의 물건 흔들기 ·
> 순위 없음 · 복귀 불가. 그리고 §15에서 폐기한 영매 직업을 「직업이 아니라 탈락 후
> 상태로」 재활용한다는 논거.
>
> **삭제된 코드:** `GhostSession`(저장소 어디에도 없다), `UI/ghost_rattle_01..04` ·
> `ghost_rattle_ready` 다섯 클립. `RacerStatus.Eliminated`는 남았지만 **접속이 끊긴
> 자리**를 뜻하도록 의미가 바뀌었다 — `RaceState.Over`가 언젠가 참이 되게 하려고
> 남아 있는 것이지, 괴물이 만드는 상태가 아니다.
>
> ⚠️ **아직 정리되지 않은 것 — 규칙은 지웠는데 미술은 반만 남았다 (2026-08-12 실측).**
> `tools/blender/gen_ghost.py`가 살아 있고 **`tools/ci/run_blender_generators.sh`의
> 기본 목록에 들어 있다.** 그런데 그것이 쓴다고 선언한
> `Assets/Models/Characters/Ghost.fbx` · `Ghost.glb`는 **저장소에 없다** — 커밋되지도,
> `.gitignore`에 걸려 있지도 않다. 반면 재료는 커밋돼 있다: `Ghost.textures.json`,
> `GhostMaterials.cs`, `GhostShot.cs`, 그리고 `Ghost_*.mat` 여러 개 — 그중 셋은
> **`Ghost_Role_Observer` · `Ghost_Role_Engineer` · `Ghost_Role_Runner`**, 즉 §04에서
> 이미 지운 직업의 이름을 달고 있다.
>
> 그러니 유령은 「지웠다」가 아니라 **「규칙에서 지웠고 미술에서 안 지웠다」**가 정확한
> 상태다. 셋 중 하나를 골라야 한다: (a) `gen_ghost`와 재료를 전부 지운다, (b) 되돌리기
> 연출(잡힌 순간의 1.75초)에 이 모델을 재사용한다, (c) 두 번째 모드를 위해 남긴다고
> 명시한다. **지금처럼 두면 v1.0이 §08에 한 것과 같은 실수다 — 반쯤 남긴 것은 없어진
> 것이 아니다.**
>
> **살아남은 것, 그리고 무엇으로 살아남았는가.** 「죽은 사람이 산 사람을 도우면 그건
> 팀이다」라는 §11의 논거는 유효하고, 이제 **더 간단하게** 유효하다. 죽은 사람이
> 없으므로 도울 사람도 없다. 그리고 「탈락하면 지루하다」는 문제 자체가 사라졌다 —
> 이것이 되돌리기 규칙이 유령 구조 전체보다 나은 이유다. 지루함을 **달랠** 장치를
> 설계하는 대신 지루해질 상태를 **없앴다.**
>
> `Assets/Audio/Presence/`의 네 클립과 `Core/Presence/`는 유령이 아니라 §10의
> **그늘**이다. 이름이 비슷할 뿐 다른 것이니 섞지 말 것.

---

## 10. 딜레마 지도

> **얻으려면 위험을 만들어야 한다.**

| 주체 | 얻는 것 | 대가 |
|---|---|---|
| 누구나 | 손전등을 켠다 | 괴물이 본다 · **남들이 내가 어디 있는지 안다** |
| 누구나 | 손전등을 끄고 다닌다 | **§10의 그늘이 고인다** — 아래 |
| **도망 중** | **뒤를 봐서 거리를 확인한다** | **속도 65% → 거리가 좁혀진다** (§05) |
| 문 | 뒤를 막는다 | 1.1초 서 있어야 하고, **내가 돌아올 수도 없다** |
| 질주 | 관문을 뚫는다 | 다음 관문에서 쓸 것이 없다 |
| 귀 | 소리를 듣는다 | 자기가 조용해야 한다 (웅크리면 느리다) |
| 지름길 | 시간을 번다 | 안쪽 고리 = 괴물의 순찰 반경 (§07) |
| 안전한 길 | 괴물을 피한다 | **모두가 나보다 앞서 있다** |
| 어그로 해제 방향 | 괴물을 떼어낸다 | 떼어낸 쪽에 있는 사람에게 **배달된다** (§06) |
| 정보 | 남에게 위치를 알려 준다 | 그 사람이 나보다 빨라진다 — **거짓말이 가능한 이유** |

마지막 세 행이 경주 고유의 것이다. 나머지는 협동판에서 그대로 왔고, 대가를 치르는
주체가 「팀 전체」에서 「나」로 바뀌었을 뿐이다.

### 그늘 — 스위치의 반대쪽 값

§03은 손전등 하나에 목표와 위험이 같이 걸린다고 말하지만, 값은 **켠 쪽에만**
매겨져 있었다 — 끄면 공짜였다. 그래서 최적 전략은 언제나 *꺼놓고 다니기*였고,
딜레마는 딜레마가 아니었다. 그늘은 어둠을 **치명적으로** 만들지 않고 **비싸게** 만든다.

| 그늘의 성질 | 값 | 무엇에 묶여 있나 |
|---|---|---|
| 가득 차는 시간 | 45 s | §12의 3회 횡단(37.5 s)보다 길다 |
| 빛으로 지우는 시간 | 15 s | 빛 1초가 어둠 3초를 산다 |
| 목소리를 잃는 시간 | 12 s | §06의 `SprintStaminaSeconds` — 질주 한 번 |
| 경고 시점 | 60 % | 남은 40 %(18 s)면 §12가 보장하는 엄폐까지 걸어간다 |
| 괴물 반경 안 | **없음** | `MonsterSightRange` 20 m — §01의 *이길 수 없는 적*은 끝까지 하나다 |

> **대가가 목소리인 이유는 v1.1에서 더 분명해졌다.** 경주에는 §13의 근접 음성이 있고,
> 목소리는 남을 속이거나 협상하는 유일한 수단이다. 그늘은 배터리도 체력도 아니라
> **말할 수 있는 능력**을 가져간다 — 스무 명이 같은 관문에 몰린 순간에.

> **왜 두 번째 *추격자*가 아닌가.** 그늘에는 위치도, 경로도, 속도도 없다. 조건이지
> 적이 아니다. §01의 공포는 *이길 수 없는 적 하나*에서 나오고, 둘이 되면 공포가
> 두 배가 되는 게 아니라 **반이 된다**. 괴물이 보는 곳(20 m)에는 그늘이 아예 없다는
> 규칙이 이걸 구조적으로 보장한다.

> 🔴 **구현 상태: 규칙과 표현은 있고, 판에는 없다.** 코어 규칙 테스트와 PlayMode
> 테스트, 모델·오디오·프리팹이 전부 통과하지만 `PresenceField`를 돌리는 것이
> 아무것도 없다 — 즉 **위 표의 대가를 실제로 치른 플레이어는 아직 없다.**

---

## 11. 인원 구조 — 최대 20인

**2~20명.** 전원이 B1 외곽 고리에 균등 배치돼 동시에 출발한다.

| 인원 | 성격 |
|---|---|
| 2~4 | 넓다. 관문에서 마주치는 일이 드물고, 괴물이 유일한 압박 |
| 5~10 | 관문마다 한두 명과 겹친다. 문이 처음으로 의미를 갖는다 |
| **11~20** | **안쪽 관문 하나에 여럿이 몰린다.** 설계 기준 |

### 인원이 늘면 무엇이 달라지나

관문 수는 인원과 무관하게 고정이다 (외곽→중간 4, 중간→안쪽 2, 안쪽→중심 1).
그래서 **인원이 늘수록 병목이 심해진다** — 스무 명이면 마지막 관문 하나를 스무 명이
지나야 한다. 그것이 이 게임이 20인에서 가장 재미있는 이유다.

> **관문 수를 인원에 맞춰 늘리면 안 된다.**
>
> 늘리면 20인 판이 20개의 1인 판이 된다. 서로 만나지 않는 경주는 경주가 아니다.
> 붐비는 것이 설계이고, 붐비는 곳에서 문과 괴물이 의미를 갖는다.

### 스무 명이 전부 같다

§04를 보라 — 직업이 없다. 스무 명은 같은 속도와 같은 손전등으로 출발하고, 차이는
전부 그 판에서 한 행동이다.

그래서 **문이 그 판의 성격을 만든다.** 앞선 사람이 관문을 닫으면 뒤 전체가 그
4.5초를 나눠 낸다. 부서진 문은 돌아오지 않으므로, 뒤로 갈수록 층은 열려 있고 대신
괴물은 §07만큼 빨라져 있다. **선두는 문을 얻고 후미는 시간을 얻는다** — 이게 이
경주가 완전히 벌어지지 않는 유일한 자동 보정이다.

> **삭제됨 — 「5개 중 4개」 구조.** v0.5의 §11은 다섯 직업 중 넷을 뽑아 하나가 항상
> 비고, 그 빈자리를 §08에서 돈으로 메우는 구조였다. 직업도 돈도 없다. 로비는 이제
> 자리 수만 센다.

### 잡힌 사람

잡히면 B1의 자기 칸으로 돌아가 **계속 달린다.** 스무 명 중 아무도 관전자가 되지
않는다 — 경주에서 죽은 사람이 산 사람을 도우면 그건 팀이고, 아무도 죽지 않으면 그
문제 자체가 생기지 않는다.

되돌아온 사람이 뒤늦게 내려가는 층은 **앞선 사람들이 이미 문을 부숴 놓은 층**이다
(위의 「선두는 문을 얻고 후미는 시간을 얻는다」). 대신 §07이 올라가 있어 괴물이 더
빠르다. 잡힌 사람에게 남는 것은 열린 길과 빠른 괴물이고, 그 둘이 상쇄되는지는
아무도 재지 않았다 — §14의 플레이테스트가 그 자리다.

---

## 12. 맵 설계 규칙

> **맵은 아트가 아니라 시스템이다.**
> §06의 수치가 맵의 **성립 조건**을 규정한다. 아래는 수치에서 역산한 규칙이다.

### 12-A. 방사 구조 — 층의 모양

한 층은 동심 3겹이다. 바깥에서 안으로만 간다. 한 층은 **한 변 57.5 m의 정사각형**이고,
여덟 층이 같은 기둥에 쌓인다.

```
   ┌─────────────────────────────────────┐
   │  외곽 고리        스무 명이 여기 선다 │
   │   ┌───────────────────────────┐     │
   │   │  중간 고리                │     │
   │   │   ┌───────────────┐       │     │
   │   │   │  안쪽 고리    │       │     │
   │   │   │    ┌─────┐    │       │     │
   │   │   │    │중심 │    │       │     │
   │   │   │    │투하 │    │       │     │
   │   │   │    └─────┘    │       │     │
   │   │   └───────────────┘       │     │
   │   └───────────────────────────┘     │
   └─────────────────────────────────────┘
```

| 경계 | 관문 수 | 이유 |
|---|:--:|---|
| 외곽 → 중간 | **4** | 스무 명이 네 갈래로 갈린다. 첫 순위가 여기서 난다 |
| 중간 → 안쪽 | **2** | 절반으로 좁힌다. 마주치면 피하기 어렵다 |
| 안쪽 → 중심 | **1** | **그 층 전원이 같은 한 칸을 지난다** |

**관문 수는 인원과 무관하게 고정이다.** §11을 보라 — 붐비는 것이 설계다.

### 12-B. 중심으로 가는 길이 어려운 네 가지 이유

거리로 어렵게 만들면 지루하고, **좁혀서** 어렵게 만들면 경주가 된다.

1. **관문이 줄어든다** — 4 → 2 → 1. 위 표
2. **관문마다 문이 있다** — 닫는 데 1.1초, 부수는 데 4.5초, **부서지면 안 돌아온다.**
   협동판에서 괴물을 막던 물건이 경주에서는 **뒤따라오는 사람**을 막는다.
   **문은 경주에 남은 유일한 상호작용이다** — `E`가 작동하는 대상은 이것뿐이다(§05)
3. **괴물이 안쪽을 순찰한다** — §07의 순찰 반경을 고리 단위로 읽는다. 외곽은 안전하고
   중심은 위험하다. 1등을 하려면 위험을 지나야 한다
4. **안쪽일수록 어둡다** — §03. 목적지는 보이는데 가는 길이 안 보인다

> **문의 산술이 그대로 경주 규칙이 된다.**
> 닫는 데 1.1초 — 그동안 나는 멈춰 있고 뒤에서 오는 사람은 달리고 있다.
> 부수는 데 4.5초 — 닫고 도망치면 **3.4초를 번다.** 숫자를 하나도 바꿀 필요가 없었다.

### 12-C. 투하구

- 중심에 **2~3개**. 각각 아래층 외곽의 **정해진** 착지점으로 간다
- 무작위가 아니다 — 맵을 아는 사람이 유리해야 실력이 성립한다
- 낙하는 되돌릴 수 없다. 잘못 고르면 그 층을 한 바퀴 더 돈다
- B8의 중심은 투하구가 아니라 **도착점**이다

### 12-D. 검증 규칙 — 그리고 도달성 프로브를 누가 물려받는가

| 규칙 | 밴드 | 이유 | 누가 재는가 |
|---|---|---|---|
| `ring-gates` | 4 / 2 / 1 | 병목이 설계대로인가 | **아무도** |
| `centre-path` | 외곽에서 중심까지 최단 **90~140 m** | 아래 | `MapValidator` — **게이트한다. 그리고 실패한다** |
| `centre-single-gate` | 안쪽→중심 관문이 **정확히 1개** | 마지막 병목이 진짜인가 | **아무도** |
| `chute-count` | 2~3 | 선택이 있는가 | **아무도** |
| `chute-landing` | 착지점이 전부 외곽 고리 안 | 미로를 건너뛰지 못하는가 | **아무도** |
| `escape-sweep` | 여덟 층 전부 **탈출 0건** | 밖으로 나갈 수 있으면 경주가 아니다 | `EscapeTests` |

> **위 문장은 오래 거짓이었다.** 이 표는 「`MapValidator`와 씬 감사기가 층마다 확인한다」로
> 시작했고, `MapValidator`는 여섯 줄 중 **한 줄도** 구현한 적이 없다. 그것이 게이트하는
> 규칙은 전부 아래 12-1의 것이고, 12-D가 새로 쓴 다섯 줄은 문서에만 있었다. 「누가
> 재는가」 칸은 그 사실이 다시 조용해지지 않게 하려고 있다.
>
> **2026-08-05에 한 줄이 참이 됐다.** `centre-path`가 `MapValidator.RuleCentrePath`로
> 들어갔다. 나머지 네 줄은 여전히 **아무도** 재지 않는다.

#### `centre-path` — 게이트한다, 그리고 층 전부가 밴드 밖이다

숫자를 가진 유일한 줄이라서 §12 재유도 때 먼저 쟀다. 그때는 재기만 했다. 지금은 게이트한다.
`MapValidator.Validate(DescentMap.Build(20260802).Graph)`, 2026-08-05:

```
[FAIL] centre-path — 외곽에서 중심까지 최단 90~140m (§12-D, 층마다)
  진입점 30개가 자기 층의 중심까지 47.5 ~ 82.5 m = 10.6 ~ 18.3 초/층 (달리기 4.5 m/s)
  30개 전부 밴드 밖:
    B1 하역장    47.5 ~ 82.5 m  (16/16 — 외곽 레일 16칸, 이 층에는 투하구가 떨어지지 않는다)
    B2~B6 · B8   70 ~ 75 m      (2/2 — 층마다 투하구 착지점 두 개)
    B7 수몰층    60 ~ 65 m      (2/2)
  밴드          90 ~ 140 m      = 20.0 ~ 31.1 초/층
```

**여덟 층 전부가 7.5~42.5 m 짧다.** 이것은 규칙의 결함이 아니라 **지도의 결함**이다 —
밴드를 측정에 맞추면 §12-D가 아래에서 직접 금지한 것을 하게 된다:
「60~90 m로 줄이면 아는 사람이 2분 만에 끝내고, 그러면 **맵을 아는 것이 실력이라는 전제**가
보상 없이 사라진다」. §01이 차이를 만드는 것으로 꼽은 두 가지 중 하나가 그것이다.

고치는 곳은 `RadialStorey`의 기하 — 외곽에서 중심까지 더 도는 길이다.

> **왜 「재기만 한다」를 그만뒀는가.** 게이트로 만들면 지도가 한 장도 안 써진다는 것은
> 사실이었고, 지금도 사실이다 — 그래서 `MapSceneGenerator.KnownFailingRules`가 **이름과
> 숫자로** 유예한다. 유예는 기록이고, 규칙을 지우거나 재기만 하는 것은 망각이다. 재기만
> 하는 숫자는 두 라운드 동안 매번 찍혔고 아무것도 막지 않았다. 이제 매 생성마다
> 「이 빌드에는 알려진 지도 결함이 있다」가 찍힌다. → [B-019](BLOCKERS.md#b-019)

**90~140 m는 재고 나서 정했다.** 처음 문서에 쓴 60~90 m는 아무것도 재기 전의 추측이었고,
그때 생성기가 실제로 만든 층은 118 m였다(방사형 이전의 생성기다). 어느 쪽이 맞는지는
판 길이가 정한다:

```
길을 아는 사람   118 m ÷ 4.5 m/s = 26초/층  ×8 = 3.5분
처음 온 사람     길을 두 배 돌면        ×2 = 7분,  문과 괴물까지 더하면 12~20분
```

§01이 잡은 12~20분에 맞는다. 60~90 m로 줄이면 아는 사람이 2분 만에 끝내고, 그러면
**맵을 아는 것이 실력이라는 전제**가 보상 없이 사라진다. 밴드를 측정에 맞췄다.

> **그리고 이 유도는 아직 잡히면 판이 끝나던 시절의 것이다.** 지금은 잡히면 B1로 돌아가
> 계속 달리므로 판 길이에 항이 하나 더 있고, 그 항의 크기(얼마나 자주 잡히는가)는 아무도
> 재지 않았다 — §14의 플레이테스트가 그 자리다. **밴드를 다시 유도할 이유는 될 수 있어도
> 낮출 이유는 아니다:** 지도는 어느 쪽 유도로도 짧고, 측정에 맞춰 규칙을 푸는 것이 이
> 문서에서 가장 나쁜 일이다.

#### 통과 기준 — 이 숫자들이 맵이 출하되는 조건이다

```
NavMesh          220 마커, 3482 쌍, 100.0% 연결, 부분 0, 섬 8
괴물 도달        212/212, 8개 층 전부
플레이어 도달    층 8/8 · 출발 36/36이 도착점에 닿음 · 투하구 14/14 · 투하구 사각 0/36
최대 오름        0.045 m  (씬 스탬프에 "-forced"가 없을 것)
탈출 스윕        여덟 층 전부 0건 (14336회 밀기 + 2304회 기울이기)
```

#### 220개 마커는 전리품이 아니라 **도달성 프로브**다

> **이것을 명시하지 않으면 v1.1에서 조용히 부서질 수 있었던 것.**
>
> §08의 전리품 마커는 §12의 **막힌 길마다 하나씩** 놓였고 — 층당 19개, 전부 152개 —
> 24개의 후보 지점과 합쳐 NavMesh 감사가 짝지어 검사하는 220개 중 176개를 차지했다.
> 즉 전리품 마커는 보상 배치인 동시에 **맵의 구석구석이 실제로 갈 수 있는 곳인지 재는
> 자**였다. 전리품을 지우면서 마커까지 지웠다면 감사가 통과한 것은 더 이상 같은 맵이
> 아니게 된다 — 그리고 그 사실은 숫자가 여전히 초록색이기 때문에 아무도 모른다.
>
> **결정: 마커는 남는다. 이름과 개수를 그대로 두고, 목적을 여기서 다시 선언한다.**
> 이제 이것들은 전리품 스폰이 아니라 **도달성 프로브**다. 런타임은 이것들을 읽지
> 않는다 — 읽는 것은 에디터의 `NavMeshConnectivity` · `PlayerTraversal` ·
> `Reachability`뿐이고, 이 셋은 씬을 마커 **이름으로** 훑기 때문에 런타임 접근자를
> 지워도 감사는 움직이지 않는다.
>
> **후속 정리(§16-4):** 생성기의 마커 접두사를 `LootSpawn_` → `Probe_`로 바꾸고,
> 감사 세 곳의 문자열을 같이 바꿀 것. **개수는 바꾸지 말 것** — 152 + 24 = 176은
> 위 표의 3482 쌍을 만드는 수이고, 이 숫자가 바뀌면 "통과"의 의미가 바뀐다.

### 12-E. v0.5에서 그대로 살아남은 규칙

미로가 미로이기 위한 규칙이므로 경주에서도 유효하다. 아래 12-1부터 그대로 읽는다.

- 20 m 직선 통로 상한 · S자 통로 · 순환로 · 막힌 길 20~25%
- 구역당 바닥 재질 1개 · 시야 차단 지점 간격 15~25 m **와 폭 14.4 m**
- 개방 공간이 최소 1개 있고 미로 공간과 **인접**해 있다 (「시야 15~25 m」 절만 빠진 채로)

**살아남지 못한 것 — 그리고 한 번 잘못 죽였다가 되살린 것.** 관측 지점 · 은폐 지점 ·
단서·목표물 후보 지점은 §04의 직업과 §03의 단서 사슬이 지워질 때 같이 지워졌다. **구역
대각선 30~40 m**는 §12 재유도에서 지워졌다 — 구역이 층보다 작다는 전제 위에 있던 규칙인데
하강에서 구역은 곧 층이다. 여기까지가 **규칙의 대상이 사라진** 경우다.

같은 재유도가 **개방 공간–미로 공간 인접**과 **시야 차단 지점 폭**도 지웠고, 그 둘은
대상이 사라진 것이 아니었다. 앞의 것은 두 절 중 한 절만 §04의 것이었고 지도는 남은 절을
지키고 있었다. 뒤의 것은 **지도가 못 지키는 것**이었다. 셋을 한 처분으로 묶는 바람에 그
라운드의 순수 결과는 **게이트 −3, +0 · 지도 변화 없음**이었다. 둘 다 돌아왔고, 하나는
통과하고 하나는 실패한다. 각각의 이유는 아래 12-1의 해당 자리에 적혀 있다.

---

### 12-1. 원래의 §12 — 괴물과 미로의 관계

### 가장 중요한 계산 — 단일 모퉁이는 실패한다

```
모퉁이 통과 시점의 거리 = D
괴물이 그 모퉁이에 도달하는 시간 = D / 4.8초
시야 차단 3초가 필요 → D ≥ 14.4m

질주로 벌 수 있는 거리 = (5.6 − 4.8) × 12초 = 9.6m  (전진 기준)
```

| 어그로 시작 거리 | 모퉁이 통과 시 거리 | 판정 |
|:--:|:--:|:--:|
| 3m | 12.6m | ❌ 실패 |
| 5m | 14.6m | ⚠️ 이론상 겨우 |
| **10m** | **19.6m** | ✅ 여유 |
| 15m | 24.6m | ✅ 안정 |

**결론 두 개.**
1. **멀리서 어그로가 붙어야 살아남는다.**
2. 단일 모퉁이에 의존하면 안 된다 — 안정적 해제에는 **연속 차단**이 필요하다.

### 연속 차단 구조 3종

#### ① S자 통로 ← 가장 확실

```
────┐
    │  ← 구간 L
    └────┐
         │  ← 구간 L
         └────

구간 L = 10m,  총 20m
통과 시간 = 20 / 4.8 = 4.2초  ≥  3초  ✅
```

**10m 구간 2개의 S자 통로 하나면 해제가 성립한다.** 이것이 맵의 기본 단위다.

#### ② 순환로
돌아서 반대편으로 나올 수 있는 구조. 괴물이 **어느 방향인지 추측해야** 한다.
경주에서는 하나 더 있다 — **남을 마주치지 않고 지나갈 수 있는 유일한 지형**이다.

#### ③ 분기
갈림길. 괴물이 잘못 고르면 완전히 놓치지만 확률적이라 신뢰도가 낮다 — 보조 수단으로만.

### 반만 삭제됨 — 15~25 m는 갔고, 인접은 남았다

원래 규칙은 이랬다. **개방 공간**은 「멀리서 어그로를 건다 · 시야 15~25 m 확보」,
**미로 공간**은 「시야를 끊고 도망친다」, 그리고 `[개방 공간] ──진입── [미로 공간]`.

**갈라 읽어야 한다. 두 절이 있고 한 절만 §04의 것이다.**

**갔다 — 「시야 15~25 m 확보」.** 이것은 한 사람의 오프닝이고, 그 한 사람은 §04의 주자다.
괴물보다 빠른 유일한 직업이었고, 일부러 보일 이유가 있는 유일한 직업이었다. §04는 삭제됐고
질주 5.6은 스무 명 전부의 것이며, 경주에서 어그로가 시작되는 방식은 §01이 따로 적어 두었다 —
**「안쪽 고리는 좁다. 마주치면 피할 수 없다」**, 이것은 아래 어그로 시작 거리 표의 ❌ 줄
바로 그것이다. **거리를 고르는 사람이 없으므로, 거리를 고르라고 만든 방이 필요 없다.**

**남았다 — 「최소 1개 있고, 미로 공간과 인접해 있다」.** 이 절은 §04에 대한 문장이 아니라
**건물에 대한 문장**이다: 층에서 유일하게 트인 방이, 같은 층에서, 떨어지는 것이 아니라
걸어 들어가는 문으로 엄폐와 이어져 있어야 한다는 것. 그리고 **지도는 이 절을 지킨다** —
`#46 B1 하역장(13,12@L0) → #47 B1_도달3`, 같은 층, 투하구 아님. 한 라운드 동안 이 규칙은
**두 절이 통째로** 지워져 있었다. 지도가 이미 통과하고 있던 게이트를, 통과하지 못하는
절과 함께 버린 것이다. 되돌렸고, 15~25 m 절은 위의 이유로 돌아오지 않는다.

지도에 남은 개방 공간은 층마다 하나, 중심의 3×3 방(7.1 m)이고 §12-C가 그 방에
요구하는 것은 15 m의 시야가 아니라 **투하구 선택이 보일 것**이다 —
「무작위가 아니다. 맵을 아는 사람이 유리해야 실력이 성립한다」. 그것은 다른 규칙이고,
이 규칙을 숫자만 줄인 것이 아니다. 방의 폭은 `MapValidator`가 여전히 **재서 찍는다** —
게이트하지 않을 뿐, 숫자가 사라지지는 않는다.

> 남은 것: `GameConstants.HallClearSightMin` 15 m. 근거가 §06으로 갈아탔다 —
> 홀을 가로지르는 사람은 괴물이 자기를 놓치는 거리(해제 거리 12 m)보다 **멀리서**
> 그것을 볼 수 있어야 한다. `Editor/Dressing`이 여전히 강제한다.

### 수치 규칙

| 항목 | 규칙 | 근거 |
|---|:--:|---|
| 시야 차단 지점 간격 | `15~25m` | 질주 60m에 3~4번의 기회 |
| **시야 차단 지점 폭** | **`14.4m 이하`** | **단일 모퉁이가 사야 하는 거리 전부. 아래** |
| S자 통로 구간 | `10m × 2회 굽음` | 통과 4.2초 > 차단 3초 |
| 순환로 개수 | 구역당 1+, 전체 3+ | 트리 구조는 사형선고 |
| 막힌 길 비율 | `20~25%` | 적으면 맵 지식 무의미, 많으면 운에 죽음 |
| **막힌 길의 대가** | **시간뿐** | 아래 |
| 구역 개수 | `4~9` | 구역 = 층. 밴드는 이제 **건물의 깊이**다 |
| 맵 한 층 | `57.5 × 57.5m` | 이 안에 고리 3겹을 관통 가능 |
| 구역 간 진입점 | `2~3` | §07 순찰 반경이 구역 단위로 성립해야 |
| **직선 통로 최대** | **`20m`** | **넘으면 쫓기는 사람이 죽는다** |

> **시야 차단 지점 폭 — 한 번 지워졌다가 다른 숫자로 돌아왔다.** 「간격 15~25 m」는 지점
> **사이**의 거리이고, 지점 **하나가 얼마나 깊어도 되는가**는 §12가 말로만 하던 절반이다.
> 상한이 없으면 4 m마다 꺾이는 60 m 복도가 「시야 차단 지점 하나」로 규칙을 통과한다.
>
> 처음 코드에 들어간 값은 `14.4 − 10 = 4.4 m`였다. 빼는 항은 §12의 어그로 시작 거리 표가
> 인정하는 10 m 선행 거리, 즉 **§04의 주자가 보일 거리를 고르는 것**이다. §01이 그 선택을
> 「마주치면 피할 수 없다」로 바꿨으므로 **그 항은 간다**. 한 라운드 동안 그 논거로 상한
> 자체가 통째로 지워졌고 규칙은 초록색이 됐다 — **지도는 1 mm도 움직이지 않았는데**.
>
> 항만 빼면 상한은 **단일 모퉁이 요구치 14.4 m 그 자체**다. 엄폐를 만나는 주자가 아무것도
> 들고 오지 않으므로, 3초를 엄폐 스스로 다 사야 한다. 그리고 지도는 **95 m** — 더 약해진
> 숫자의 **6.6배**다. 기하가 못 지키는 게이트는 잘못된 게이트가 아니라 **지도의 결함**이고,
> 이름과 숫자로 유예한다. → [B-007](BLOCKERS.md#b-007)

> **삭제됨 — 구역 대각선 30~40 m.** 위 표에서 유일하게 **근거 칸이 비어 있던 줄**이다.
> 이 규칙은 구역을 **층보다 작은 구획**으로 놓고 크기를 정했고, §12의 나머지가 대는
> 두 이유도 전부 그 전제 위에 있다 — 「발소리가 방을 지목할 만큼 작게」(소리 → 바닥 재질)와
> 「주자가 구역 2~3개 관통 가능」(맵 한 층).
>
> 하강에서 **구역은 곧 층이다.** 여덟 구역, 여덟 층, 표면 하나씩. 그러면 두 이유가 다
> 뒤집힌다. §01은 발소리가 **층**을 지목하게 만드는 것이 설계이므로(「여덟 개의 표면,
> 층마다 하나」) 층당 3~4개의 구역은 층당 3~4개의 표면을 뜻하고 그것은 청각 지도를
> 섬기는 게 아니라 부수는 것이다. 그리고 「구역 2~3개 관통」은 한 번의 질주로 **층 두세
> 개**를 지난다는 뜻이 되는데, §01이 구조적으로 금지한다 — 층은 오직 중심의 투하구로만
> 벗어난다. `DescentMap.Radius` 11이 모든 층을 57.5 m 정사각형, 대각선 **81.3 m**로
> 만들므로 이것은 밴드를 조금 벗어난 지도가 아니라 **밴드가 말하는 대상이 아닌 지도**다.
>
> 층에서 얼마나 걸어야 하는지는 여전히 묶여야 하고, 그 규칙은 §12-D의 `centre-path`
> 90~140 m다 — 이제 `MapValidator`가 **게이트한다**. 상수 `ZoneDiagonalMin/Max`는 남는다 —
> 복제 범위 · 수색 웨이포인트 · 그림자 거리 · 안개가 그 40 m를 **참조 거리**로 읽고 있고,
> 그중 §12를 주장하는 것은 하나도 없다.
>
> **이 줄만 진짜로 지워졌다.** 같은 라운드에 함께 지워진 다른 둘은 지워질 것이 아니었다 —
> 하나는 절 하나만 §04의 것이었고(위 「반만 삭제됨」), 하나는 기하가 못 지키는 것뿐이었다
> (위 「시야 차단 지점 폭」). **규칙의 대상이 사라진 것**과 **지도가 못 지키는 것**은
> 같은 처분을 받지 않는다: 앞의 것은 논거와 함께 지우고, 뒤의 것은 이름과 숫자로 유예한다.

> **「막힌 길 보상 — 전리품 · 자재」가 삭제되었다.** 놓을 것이 없다. 그래서 막힌 길의
> 대가는 이제 **오직 잃어버린 시간**이고, 그게 경주에서는 충분하다 — 20초를 잃으면
> 스무 명 중 몇 명이 나를 지나간다. 협동판에서는 보상이 필요했다. 위험을 감수할
> *이유*를 따로 만들어 줘야 했기 때문이다. 경주에는 이유가 이미 있다: **이기고 싶다.**

### 소리 → 바닥 재질이 지도다

| 층 | 이름 | 바닥 | 발소리 |
|:--:|---|---|---|
| B1 | 하역장 | 콘크리트 | 둔탁 |
| B2 | 기록보관소 | 나무 | 삐걱 |
| B3 | 기계실 | 금속 | 울림 |
| B4 | 저탄장 | 자갈 | 부스럭 |
| B5 | 저수조 | 타일 | 딱딱, 반향 |
| B6 | 병동 | 카펫 | 먹먹 |
| B7 | 수몰층 | 물 | 첨벙 |
| B8 | 굴착층 | 흙 | 퍽퍽 |

**층마다 바닥 재질이 달라야 발소리로 위치를 판별할 수 있다.** 경주에서는 괴물뿐
아니라 **남들이 어느 관문에 몰려 있는지**를 이것으로 안다. 아트 결정이 아니라
**시스템 결정**이다. 재질 경계를 명확히 할 것.

> **v0.5의 「구역 A~D + 계단」 표를 층 단위로 바꿨다 (2026-08-12).** 예전 표는 한 층
> 안에서 구역을 나누던 협동판 구조였고, 다섯 칸이었다. 지금 출하되는 건물은
> `Editor/SceneGen/DescentMap.cs`의 `Storey` 표가 정하며 **여덟 층에 여덟 재질을 하나씩**
> 쓴다 — 주석 그대로 "one per storey, so a footstep names a floor". 발소리가 층을
> 부르는 쪽이 경주에 맞다: 내려가는 게임에서 알아야 하는 것은 *어느 구역*이 아니라
> **몇 층**이다. `Core/Map/FloorMaterial.cs`가 여덟을 선언하고
> `Assets/Audio/Footsteps/`가 96개(8 재질 × 3 주체 × 4 변형)를 싣는다.
>
> **다만 8×8 분리 여유가 거의 없다.** 2026-08-12 `tools/ci/verify_audio.sh` 측정:
> 최악 쌍 **water vs gravel 1.44×**(기준 1.4×), 같은 주체 안에서 **1.41×**. 아홉 번째
> 재질을 추가하면 이 게이트가 깨질 가능성이 매우 높다.

> 이 표는 원래 §04 청음사의 요구조건이었다. 직업이 없어졌는데 규칙이 살아남은 이유는
> 간단하다 — **귀는 스무 명 전부가 가지고 있다**(§04). 청중이 스무 배가 됐다.

### 관측 지점
높이 차(2층 난간) · 창문 · 격자 등 **15m 거리에서 안전하게 괴물을 볼 수 있는 지점**을
**구역당 1~2개** 배치. 안쪽 고리를 들어가기 전에 안을 살필 수 있는 자리다.
경주에서는 **누가 어느 관문으로 갔는지**를 보는 자리이기도 하다.

### 병목에 문
- 순환로의 목에 문 하나 → 잠그면 순환이 끊김 (전략적 선택)
- **구역당 잠글 수 있는 문 1~2개** — 많으면 선두가 층 전체를 봉쇄한다
- 문의 위치는 `MapValidator`가 검사한다: 그 문을 닫았을 때 강제되는 우회 거리가
  §06의 해제 시간보다 길어야 한다

> **삭제됨 — 전기 패널 구역당 1개.** §03에서 조명 관리를 없앴으므로 켤 것이 없다.

### 좁은 공간
넓은 곳에서는 괴물이 우회한다. 미로 공간이 무대이므로 별도 요구조건은 없다.

> **삭제됨 — 「단서 · 목표물 후보 지점 조건」 전체.** 탈출로 2개 이상 · 입구에서 내부
> 불가시 · 근처 시야 차단 · 전기 패널 접근이라는 네 조건으로 구역당 3개의 후보를 만들고
> 그중 하나를 뽑던 규칙이었다. 목적지가 처음부터 알려진 경주에는 후보가 없다.
> 남은 24개의 후보 지점 마커는 위 12-D의 **도달성 프로브**로 용도가 바뀌었다.

### 검증 체크리스트

**여덟 줄이고, 여덟 줄 전부 `MapValidator`가 검사하고, 지금 지도는 여덟 줄 전부 통과한다.**
한때 열한 줄이었다 — 관측 지점 · 단서·목표물 후보 지점 · 은폐 지점은 §04의 직업, §03의 단서
사슬, §07 새벽의 매복이 지워질 때 **대상이 사라져서** 같이 지워졌다.

- [x] 20m 넘는 직선 통로가 없다 — 17.5 m
- [x] 개방 공간이 최소 1개 있고, 미로 공간과 인접해 있다 — `#46 → #47`, 같은 층
- [x] S자 통로(10m×2)가 구역당 최소 1개 있다
- [x] 순환로가 맵 전체에 3개 이상 있다 — 95개
- [x] 막힌 길 비율이 20~25%다 — 152/720 = 21.1%
- [x] 구역별 바닥 재질이 다르고 경계가 명확하다 — 여덟 층, 여덟 표면
- [x] 잠글 수 있는 문이 구역당 1~2개, 병목에 있다 — 층마다 1개
- [x] 구역 간 진입점이 2~3개로 제한돼 있다 — 인접한 층마다 2개

수치 규칙 쪽(위 표와 12-D)은 그렇지 않다. **여섯 줄 중 다섯이 통과하고 하나가 실패한다.**

- [x] 구역 개수 4~9 — 8
- [x] 맵 한 층 — 57.5 × 57.5 m
- [x] 맵이 하나로 연결돼 있다 — 한 덩어리
- [x] 각 지점이 자기 구역 안에 있다
- [x] **시야 차단 지점 폭** — 가장 깊은 곳이 **12.5 m**, 상한 14.4 m ·
      [B-007](BLOCKERS.md#b-007) **닫힘** (`9f0f447`)
- [ ] **`centre-path` 외곽→중심 90~140 m** — 진입점 **22개 중 21개**가 밴드 안,
      남은 하나가 **2.5 m** 모자란다 · [B-019](BLOCKERS.md#b-019)

**유예는 이제 한 줄이다.** `MapSceneGenerator.KnownFailingRules`에 남은 항목은
`MapValidator.RuleCentrePath` 하나뿐이고, 지도는 그 유예 위에서 써진다. 시야 차단 지점
**간격** 15~25 m 쪽도 통과한다.

> 🔴 **이 목록은 2026-08-12에 다시 쟀다.** 예전 판은 시야 차단 지점 폭을 **95 m**로,
> `centre-path`를 **진입점 30/30 실패**로 적었고 「둘이 실패한다」고 했다. 둘 다 그때
> 사실이었고 지금은 아니다. 근거는 코어 스위트에 이름으로 박혀 있다 —
> `MapTests.Descent_MeetsSection12sSightBreakSpacing`이
> `"the deepest 12.5 m (cap 14.4 m)"`를 문자열로 검사하며 **통과**하고,
> `Descent_CentrePath_IsInsideSection12DsBandExceptAtTheRimCellBesideAGate`가 남은
> 2.5 m를 고정한다. 2026-08-12 실행: `MapTests.Descent_` 필터 **4개 전부 통과, 36초**.
>
> ⚠️ **위의 「152/720」과 「720 지점, 814 통로」는 다시 재지 않았다.** 지점·통로 수를
> 세려면 Unity 에디터가 필요하고 이 감사에서는 열 수 없었다. 두 숫자는 **재저작 이전
> 건물**의 것일 가능성이 높으니, 인용하기 전에 `MapPipeline`으로 다시 뽑을 것 —
> `MapSceneGenerator.GenerateFromCommandLine`은 배치·데칼·발광을 하지 않으므로 거기서
> 나온 수는 게임의 건물이 아니다.

12-D의 나머지 두 줄은 `MapValidator`가 아니라 씬 감사기가 잰다.

- [x] 도달성 프로브 마커가 막힌 길마다 하나씩 있다 — 176/176 (12-D)
- [x] 밖으로 나가는 길이 없다 — 탈출 스윕 0건 (12-D)

### 실전 검증 — 탈출 **대가**

> **바뀐 것은 숫자가 아니라 질문이다.** 원래 이 자리에는 「맵의 임의 지점에서 어그로를
> 걸고, **해제할 수 있는가**? 10개 지점 중 5~7개면 적정」이 있었다.
>
> **「도망칠 수 있다」는 「죽지 않는다」는 뜻이었다.** §06은 잡힌 사람의 판을 끝냈고
> §02는 순위를 주지 않았으니, 5~7/10은 *플레이어가 계속 플레이하는 비율*에 관한
> 문장이었다. 지금 잡히면 **B1의 자기 칸으로 돌아가 계속 달린다** — 가지고 있던 층을
> 전부 잃고, 그 외에는 아무것도 잃지 않는다. 탈출은 더 이상 게임을 하느냐 마느냐의
> 갈림이 아니고, 따라서 **탈출 성공률은 등급이 아니다.**
>
> 그리고 그 밴드는 애초에 판별력이 없었다. §04가 삭제되면서 질주 5.6이 전원의 것이
> 됐고, 「도망칠 수 있는가」에는 §06의 산술이 **모두에게 똑같은** 답을 이미 준다:
> 시야가 이어져 있으면 한 모퉁이 뒤로 16.8 m만 가면 해제된다. §12는 구역당 10 m×2의
> S자 통로를 요구하고 직선을 20 m로 자르고 순환로를 3개 이상 요구한다 —
> **§12의 건설 규칙을 지킨 지도는 §12의 실전 검증에 실패할 수 없다.** 그 밴드는 §12의
> 나머지가 금지하는 지도를 묘사한다. (측정 3회, 등급은 10/10에서 움직인 적이 없다.)

**경주가 묻는 것: 떼어내는 데 얼마가 들었는가?** §07이 「시간이 유일한 통화다」이고
§02가 「먼저 닿는다」이므로, 등급의 단위는 초다.

```
탈출 대가 = 어그로부터 해제까지의 시간
          + 되돌려 준 거리 ÷ 달리기 4.5 m/s
```

되돌려 준 거리는 **자기 층의 중심까지**로 잰다 — §01이 층마다 독립된 미로로 만들었고
투하구는 길이 아니라 낙하이므로.

| 밴드 끝 | 값 | 유도 |
|---|:--:|---|
| **바닥** | **3.4초** | §01은 남을 방해하는 수단을 정확히 둘만 남겼다 — 「**문**과 **괴물을 남에게 떠넘기는 것**뿐이다」. 앞의 것 값은 정해져 있다: 닫는 데 1.1초, 부수는 데 4.5초, **차이 3.4초**. 뒤의 것이 문보다 못한 수단이면 §01의 목록은 사실 한 줄이다. |
| **천장** | **20.0초** = 한 층 | 잡히면 B1로 돌아가 **가지고 있던 층을 전부** 잃는다. 즉 B*k*에서 잡히는 값은 *k*개 층이고, **가장 싼 잡힘은 B1의 한 층**이다. 탈출이 그것보다 비싸면 도망치지 않는 것이 합리적인 선택이 된다. 한 층 = §12-D의 최단 합법 중심 경로 90 m ÷ 4.5 = 20.0초. |

천장이 **층마다 다르다**는 것이 이 유도의 핵심이다. B6에서 25초를 내고 빠져나오는 것은
6개 층을 잃는 대신이므로 싸다. B1에서 25초는 비싸다. 상수는 가장 빡빡한 쪽(B1)을 쓴다.

> ✅ **이 유도가 기대는 규칙이 이제 §01 · §02 · §06 · §09 · §11에 적혀 있다
> (2026-08-12).** 이 문단은 세 절의 표가 아직 「잡히면 — **탈락**」이라고 말하던
> 시절에 그 사실의 임시 주소로 쓰였다. 코드는 그때도 지금도 이렇다:
> `MatchDirector.SendBackToTheStartLine()`이 주자를 `RaceRunners.LocalStart` —
> B1에서 자기가 출발한 칸 — 으로 옮기고, `MatchDirector.CaughtGraceSeconds = 1.75f`
> 동안 §06이 다시 잡지 못한다(없을 때 스무 명 판에서 한 명이 135초에 245번 잡혔다).
> `RaceState.ReportCaught`는 상태를 `Running`으로 두고 층을 `0`으로 되돌린다.
>
> **교훈은 문서 쪽에 있다.** 규칙 하나가 코드에서 바뀐 뒤 **다섯 절**이 옛 규칙을
> 말하는 상태로 며칠을 갔고, 그동안 이 문단만이 사실을 알고 있었다. 한 곳이 「다른
> 절이 틀렸다」고 적어 두는 것은 응급 처치이지 수정이 아니다.

#### 측정 — seed 20260802, 720개 지점 전부, 심야 4.8 m/s

> ⚠️ **이 블록 전체가 「720개 지점」 건물의 것이다** (위 체크리스트의 경고와 같은
> 이유, 2026-08-12). 시드 `20260802`는 지금 `DescentRoster.txt`에서 **B8
> (`Map_Descent_7`)** 한 층의 시드이고, 재저작 이후 지점 수는 다시 세지 않았다.
> 아래 비율과 고리별 표는 **경향**으로 읽고, 숫자를 인용해야 하면 `MapPipeline`으로
> 다시 뽑을 것.

```
탈출 대가     최소 3.4  ·  p25 6.0  ·  중앙값 7.2  ·  p75 9.2  ·  최대 25.5 초
바닥 미만     0 / 720          ← 괴물은 어디서도 무시할 수 있는 것이 아니다
평면 천장 초과  7 / 720 (1.0%)   전부 중심 칸, 층마다 하나 (B8 제외)
층별 천장 초과  1 / 720          B1의 중심 25.5초 대 20.0초  ← 유일한 진짜 위반
```

§12-B③ 「외곽은 안전하고 중심은 위험하다」가 처음으로 숫자가 됐다:

| 고리 | 지점 | 되돌려 준 거리(중앙값) | 대가(중앙값) |
|---|:--:|:--:|:--:|
| 중심 d0-1 | 72 | **+15.0 m** | 9.0초 |
| 안쪽 d3-4 | 88 | +12.5 m | 7.9초 |
| 중간 d6-7 | 200 | 0.0 m | 7.8초 |
| 외곽 d9-10 | 224 | **−7.5 m** | 6.6초 |

중심에서 떼어내면 중심 밖으로 던져지고, 외곽에서 떼어내면 오히려 안쪽으로 실려 간다.
**성공률은 이 표의 모든 줄을 「100%」 한 칸으로 뭉갠다.**

> **§07의 가격표와 대조.** §07은 「괴물을 한 번 떼어내기 ~30초」라고 적어 두었다. 위
> 중앙값은 7.2초지만, 그 탈출이 쓴 스태미나를 되채우는 값(회복 20초 × 쓴 비율 + 지연
> 1초)을 더하면 **중앙값 18.4초 · p75 23.6초**가 된다. §07의 추정치는 바(bar)까지 포함한
> 한 판 전체이고, 위 밴드는 **논란의 여지 없이 잃은 시간**만 묶는다. 밴드의 유도는 §01·§06
> 이고 이 대조는 확인일 뿐이다 — §07의 30초는 유도가 아니라 추정이므로 게이트로 쓰지 않는다.

#### 등급이지 게이트가 아니다

원래대로다. §12는 체크리스트를 게이트로, 실전 검증을 등급으로 나눴고 그 분리는 옳다 —
등급이 나쁘다고 생성을 막으면 나쁜 점수를 받은 지도를 **볼 수가 없다.**
`MapSceneGenerator`가 생성할 때마다 위 블록을 찍는다.

> **남아 있는 의심 — 정직하게.** 대가는 그래프 위에서 잰다. 엔진의 레이캐스트는
> 그래프만큼 관대하게 엄폐를 주지 않는다(`MonsterChaseTests`: S자 통로는 해제, 단일
> 모퉁이는 **잡힘**). 그리고 `RunnerTest.Explore`는 DFS 순서로 **처음** 해제되는 경로를
> 돌려주므로 위 값은 *임의의* 탈출의 대가이지 길을 아는 사람이 고를 *가장 싼* 탈출의
> 것이 아니다 — 실제 대가는 위 중앙값 **이하**다.

---

## 13. 인프라와 기술 스택

> **직접 띄울 서버가 0대. DB도 필요 없다. 월 고정비 0원, 초기 비용 $100.**
>
> 호스트 권한 구조에서는 "서버"가 곧 한 명의 플레이어 PC이므로 Steam이 나머지를 전부 제공한다.

### Steam이 무료로 주는 것

| 기능 | Steamworks | 비용 |
|---|---|:--:|
| **P2P 네트워킹** | Steam Networking Sockets | 0 |
| **NAT 통과 + 릴레이** | Steam Datagram Relay (SDR) | 0 |
| 로비 / 친구 초대 | ISteamMatchmaking | 0 |
| **음성 캡처 + 코덱** | ISteamUser::GetVoice | 0 |
| 계정 / 신원 | Steam ID | 0 |
| 세이브 | Steam Cloud | 0 |
| 업적 · 통계 · **리더보드** | Steam Stats / Leaderboards | 0 |
| 배포 · 패치 | Depot upload (steamcmd) | 0 |

**릴레이가 무료인 것이 결정적이다.** NAT 뒤의 플레이어끼리 직접 연결이 안 될 때
트래픽을 중계해야 하는데, 직접 하면 대역폭 요금이 실제로 나간다.

**리더보드가 경주에서 값이 올랐다.** 선착순 게임은 기록이 남아야 다시 하고, Steam이
그것을 공짜로 준다. 층별 최속, 완주 시간, 판당 순위 — 전부 Steam Leaderboards로 된다.

따라서 **전용 서버 · 매칭 서버 · 릴레이 서버 · 계정 서버 · DB 전부 불필요하다.**

### DB가 필요할 것 같은 것들

| 필요해 보이는 것 | 실제 해법 | DB |
|---|---|:--:|
| 계정 · 프로필 | Steam ID | ❌ |
| 세이브 | Steam Cloud | ❌ |
| 리더보드 | Steam Leaderboards | ❌ |
| 매칭 · 방 목록 | Steam Lobbies | ❌ |
| 업적 · 통계 | Steam Stats | ❌ |
| **영구 진행도 · 인벤토리** | **없음 — 한 판 안에서 완결된다** | ❌ |

마지막 항목이 중요하다. **§15에서 판 사이 메타 프로그레션을 폐기한 것이
인프라 이득으로 돌아온다.** 영구 진행도가 있으면 저장해야 하고, 저장하면 치팅 검증이
필요해지고, 그러면 진짜 서버가 필요해진다. §08을 지운 v1.1은 이 결정을 한 번 더
굳혔다 — 이제 판 안에서도 저장할 상태가 없다.

### 근접 음성 구현

```
① 마이크 캡처       →  ISteamUser::GetVoice()
② 압축 패킷 전송     →  Steam Networking (P2P)
③ 수신 후 압축해제   →  DecompressVoice()
④ 재생             →  송신자 캐릭터 위치의 3D 오디오 소스로
```

> **핵심 트릭: 음성을 3D 오디오 소스로 재생하면 근접 음성이 자동으로 된다.**
> 거리 계산 로직이 필요 없다. 엔진의 3D 오디오가 감쇠와 벽 차폐를 처리한다.

```
거리 > 30m  →  전송 자체를 중단
```

**전부 받아놓고 볼륨만 0으로 재생하면 클라이언트 조작으로 다 들린다.**
전송 단계에서 끊어야 한다 — 대역폭 절감보다 **도청 방지**가 본질이다.
경주에서 도청은 협동판보다 훨씬 큰 이득이므로, 이 결정은 더 중요해졌다.

### 텔레메트리 — 3단계, DB 없음

§06 수치는 **실제 사람들 데이터 없이는 못 맞춘다.** 하지만 요구사항을 보면
`판 종료 시 요약 1건 전송 → 나중에 집계` 로, **append-only 로그**다.

#### 1단계: Steam Stats만 (인프라 0)

통계 항목을 정의하면 Steam이 저장하고 글로벌 집계까지 해준다.
**분포가 필요하면 버킷 카운터를 만든다.**

```
aggro_duration_0_5s     : 카운터
aggro_duration_5_10s    : 카운터
aggro_duration_10_15s   : 카운터
aggro_duration_15s_plus : 카운터
```

| 버킷 | 검증 대상 |
|---|---|
| 어그로 지속 시간 | §06 해제 거리 12m |
| **층별 통과 시간** | §12-D의 90~140 m 밴드 |
| 후진(S) 사용 시간 비율 | §05 곁눈질 배율 65% |
| **문을 닫은 횟수 / 부순 횟수** | §12-B — 문이 실제로 쓰이는가 |
| **탈락 / 완주 / 시간 초과 비율** | §02 · §07 전체 밸런스 |

> **삭제된 버킷:** 왕복 횟수, 직업별 선택 카운터 5개, 아이템별 구매 카운터.
> 셀 것이 없다.

#### 2단계: 로그 파일 (세부 이벤트가 필요해지면)

사망 좌표처럼 세부 데이터가 필요해지면:

```
서버리스 함수 1개  →  오브젝트 스토리지에 JSONL append
                     (S3 / R2 / Blob)
```

**DB가 아니라 파일이 쌓인다.** 나중에 다운로드해서 로컬 DuckDB로 쿼리한다.

#### 3단계: 분석 서비스 (규모가 커지면)

PostHog 등을 붙인다. 이때도 DB는 서비스가 관리하고 나는 이벤트만 쏜다.

> 익명 세션 ID만 쓴다. **개인정보는 수집하지 않는다.**

### 기술 스택 (Unity)

| 영역 | 선택 | 근거 |
|---|---|---|
| 엔진 | **Unity 6000.3** | 네트워킹 + 3D 오디오 + Steam 연동 생태계가 가장 성숙 |
| Steamworks | Steamworks.NET | 저수준 래퍼, 안정적, 무료 |
| 네트워킹 | **Mirror + FizzySteamworks** | 자료·커뮤니티 최다 — **막혔을 때 검색이 된다** |
| 오디오 | Unity 기본 → 필요시 FMOD | 발소리 방향 판별 가능 여부를 프로토타입에서 판정 |
| 개발용 App ID | `480` (Spacewar) | 심사 전에도 로비·P2P·음성 테스트 가능 |

> **20인은 4인이 아니다.** v0.5는 「4인 게임에서 성능 한계에 부딪힐 일은 없다」고
> 적었다. 스무 명이 같은 층에 있으면 관심 관리(interest management)와 음성 대역폭이
> 실제 문제가 된다. Mirror의 관심 영역을 §12의 구역 단위로 잘라 두는 것이 첫 대응이고,
> 음성은 위의 30 m 컷이 이미 그 역할을 한다. **§16-1의 리스크로 올린다.**

### 아키텍처 — 호스트 권한

| 항목 | 결정 |
|---|---|
| 권한 모델 | **호스트 권한** — 괴물 AI를 호스트가 돌린다 |
| **순위 · 도착 판정** | **호스트만.** §02 — 경주에서 가장 먼저 조작되는 값이다 |
| 치팅 방어 | 순위 판정 외에는 최소한으로 |
| 호스트 이탈 | **세션 종료** — 마이그레이션 하지 않음 |

> **삭제된 행:** 「단서 내용 · 목표물 위치 — 호스트만 보유」. 숨길 정답이 없다.
> 대신 그 자리에 순위 판정이 들어왔고, **숨기는 것에서 판정하는 것으로** 호스트의
> 역할이 바뀐 것이 v1.1의 성격을 잘 보여 준다.

### 인프라가 아니라 행정 — 세팅해야 할 것

| 항목 | 비고 |
|---|---|
| Steamworks 파트너 등록 | 사업자 정보 · 은행 계좌 |
| **App ID** | **$100** (매출 $1,000 초과 시 환급) |
| W-8BEN 제출 | 한미조세조약으로 원천징수 감면 |
| Depot 설정 + 업로드 | 초기엔 수동 `steamcmd`로 충분 |
| **상점 페이지 자산** | 캡슐 이미지 · 트레일러 · 스크린샷 — **시간이 꽤 든다** |
| 헤드폰 권장 표기 | §05 — 3D 오디오가 게임의 핵심 |

### 나중에 서버가 필요해질 수 있는 경우

| 상황 | 필요해지는 것 |
|---|---|
| 스팀 외 플랫폼 출시 (에픽 등) | 자체 매칭 · 음성 추상화 레이어 |
| 공개 매칭 (친구 아닌 사람과) | Steam Lobbies로 가능하나 신고 · 필터 시스템 |
| **랭크전** | 순위를 검증할 권위 — 호스트 권한으로는 부족 |

앞의 둘은 지금 불필요하다. 셋째는 경주 게임이 성공하면 반드시 요구되는 것이므로,
**음성 · 네트워킹 · 순위 판정을 인터페이스로 추상화**해 두면 나중 확장 비용이 줄어든다.

---

## 14. 개발 순서

| # | 내용 | Steam |
|:--:|---|:--:|
| 1 | 1인칭 컨트롤러 + **속도 배율(§05)** + 괴물 AI + 어그로 | — |
| 2 | **방사형 층 생성기 + 투하구** (§12) | — |
| 3 | **경주 규칙** — 승패 · 순위 · 탈락. §02를 코드로 | — |
| 4 | Mirror 로컬 호스트 — 같은 PC 2인스턴스 | — |
| 5 | **20인 스폰 · 관심 관리 · 호스트 순위 판정** | `480` |
| 6 | **근접 음성** | `480` |
| 7 | 텔레메트리 (Steam Stats) · **리더보드** · 크래시 리포팅 | — |
| 8 | **Steam 상점 페이지 · 위시리스트 수집** | 실제 ID |

> **경고: 마지막 항목을 늦추지 말 것.**
> 상점 페이지는 게임 완성 전에 올려서 위시리스트를 모으는 용도다.
> 출시일 알고리즘 노출이 여기 걸려 있고, 스팀에서 가장 흔한 실수다.

### 2주 프로토타입

**만들 것**
- 방사형 층 **2개** (외곽/중간/안쪽 + 투하구 하나)
- **1인칭 이동 + 속도 배율표**
- 질주 + 문
- 괴물 1마리, 어그로 + 상태 기계
- 시계 + 시간대별 위협 2단계
- 순위 판정 (도착 순서만)
- **4인 로컬** (캐릭터는 캡슐) — 문 앞에서 붐비는지 보려면 최소 넷은 필요하다

**안 만들 것**
- 20인 · 관심 관리
- 게임 내 음성
- 유령 · 그늘
- 캐릭터 모델 · 애니메이션
- 매칭 · 로비 · 리더보드
- 아트 · 사운드 폴리시

### 검증 질문 — 이 다섯 개만

| # | 질문 | 실패 시 |
|:--:|---|---|
| 1 | **추격이 재밌는가?** (거리를 벌리는 순간이 짜릿한가) | §06 어그로 수치를 고친다 |
| 2 | **곁눈질 딜레마가 작동하는가?** (뒤를 볼지 고민하게 되는가) | §05 후진 배율(65%)을 조정 |
| 3 | **관문에서 붐비는 것이 재밌는가?** (문을 닫고 갈지 고민하는가) | §12-A 관문 수 4/2/1을 조정 |
| 4 | **B1로 되돌려지는 것이 억울하지 않고 납득되는가?** (그리고 되돌아온 뒤 계속 달릴 마음이 드는가) | §06 순찰 반경 · §07 시간 곡선 · `CaughtGraceSeconds` |
| 5 | **발소리의 방향·거리를 구별할 수 있는가?** | FMOD를 붙인다 |

> **우선순위:** 1·2번이 안 되면 나머지를 아무리 잘 만들어도 안 된다.
> 3번이 경주 전환으로 새로 들어온 질문이고, 이 게임이 20인에서 재미있는지를 정한다.
> 그리고 이건 문서로 정할 수 없고 직접 만져봐야 나온다.

---

## 15. 설계 이력

이 설계에 도달하기까지 버린 것들. 되돌아가지 않기 위해 기록한다.

| 폐기안 | 폐기 이유 |
|---|---|
| 런타임 LLM 활용 | 매절 판매 + 변동비 = 잘 팔릴수록 적자. 지연시간도 실시간 액션과 맞지 않음 |
| 영매 / 침식 구조 *(감각 비대칭)* | 화면이 검은 역할은 **아무도 하고 싶어하지 않는다.** §09가 이것을 탈락 상태로 한 번 회수했으나, 그 §09도 삭제됐다 — 되돌리기 규칙에는 탈락이 없다. **두 번 폐기된 유일한 항목** |
| 사수 (총기 직업) | 괴물을 죽일 수 있으면 공포가 사라진다. 저지는 전부 일시적이어야 함 |
| 해독가 (목표 전담) | 목표를 한 명이 담당하면 나머지는 호위병이 된다 |
| 의무병 / 서포터 | 필수인데 재미없는 역할 |
| 시간 기반 어그로 해제 | 기다리는 것은 실력이 아니다. 거리 + 시야 차단으로 대체 |
| 적응형 보스 패턴 | 고무줄 효과로 실력 성장의 보상이 사라진다. 대신 랜덤화 + 시간 기반 위협(§07) |
| 의식 복원 (목표 구조) | **서사 장치라 반복하면 빨리 낡는다** |
| 잠입 횟수 기반 위협도 | 나가서 쉬는 것이 공짜가 되고 인공적 카운터가 필요 |
| "벽에 부딪힌다"는 뒤보기 페널티 | 애매한 서술. **후진 속도 65%라는 수치로 대체**(§05) |
| 전용 서버 / 자체 릴레이 | Steam SDR이 무료로 해준다 |
| **텔레메트리용 DB (Supabase 등)** | **append-only 로그에 DB는 과하다** |
| 판 사이 메타 프로그레션 | 한 판 안에 완결된다. 나중에 추가 가능 |
| **4인 비대칭 협동 회수 (v1.0에서 폐기)** | **오너의 한 문장으로 끝났다: 「그냥 선착순 미로탈출게임이야…..! 귀신피해서 지하로 들어가서 나가는」** |
| **다섯 직업 (v1.0)** | 픽이 있으면 진 이유를 픽에서 찾는다. 20인은 같은 몸이어야 한다 (§04) |
| **왕복 · 지상 · 상점 · 전리품 · 크레딧 (v1.0에서 결정, v1.1에서 삭제)** | 경주에 재보급이 없고 통화도 없다 (§08) |
| **단서 3층위 (v1.0에서 결정, v1.1에서 삭제)** | 목적지가 처음부터 알려져 있다 (§03) |
| **배터리 · 배전반 · 발전기 (v1.1)** | **유지해야 하는 빛은 심부름이다.** 어둠은 남기고 관리를 없앴다 (§03) |

> **v1.0과 v1.1의 차이는 "결정"과 "삭제"의 차이다.**
> v1.0은 위 세 줄을 결정했고 코드를 게이트로 막아 두었다. 그 상태로 며칠이 지나자
> 상점 객체가 경주 첫 틱에 생성되고, 전리품 상태가 매 고정 스텝 동기화되고, 맵은
> 152개의 전리품 마커를 계속 배치하고 있었다. **막아 둔 것은 없어진 것이 아니다.**
> 다음에 이런 규모의 방향 전환을 하면, 문서를 고치는 커밋과 코드를 지우는 커밋을
> 떼어 놓지 말 것.

> **설계 관통 원칙**
>
> **패턴은 학습하게 두고, 내용을 매번 바꾼다.**
> 구조를 배우는 것이 실력이고, 그 실력이 통하지 않게 만들면 게임이 죽는다.
> 공략을 막으려 하지 말고, **공략을 봐도 남에게 전이되지 않는 축**을 만들 것.
> 경주에서 그 축은 **다른 열아홉 명**이다. 맵을 외워도 그들이 어디서 문을 닫을지는 모른다.

---

## 16. 미결 과제

| # | 과제 | 상태 |
|:--:|---|---|
| 1 | **20인 동시 접속** — 관심 관리 · 음성 대역폭 · 호스트 부하 (§13) | 🔴 최우선 |
| 2 | **괴물 모델 · 애니메이션 + 플레이어 모델** — 프로젝트의 숨은 병목 | 🔴 리스크 |
| ~~3~~ | ~~`ClueMinReadableLightQuality` 개명~~ — ✅ **완료.** `MinSafeLightQuality` = 0.20f (§03) | — |
| 4 | 도달성 프로브 마커 접두사 정리 `LootSpawn_` → `Probe_` (§12-D) | 🟡 중 |
| 5 | 질주 스태미나 총량과 회복 속도 (§04) | 🔴 상 |
| 6 | 투하구 2개 대 3개 — 선택지가 실제로 갈리는가 (§12-C) | 🟡 중 |
| 7 | 맵 개수 (얼리 액세스 시 3개 권장) | 🟢 하 |
| 8 | 가격 (9,900~12,000원 권장) | 🟢 하 |
| — | ~~어그로 수치 · 조작과 속도 배율 · 사망 처리 · 순찰 패턴 · 엔진 · 인프라 · 맵 규칙 · 시간 위협 · 승패 조건 · 인원 구조~~ | ✅ 완료 |
| — | ~~경제 가격표 · 전리품 가치 · 단서 구조 · 목표물 형태 · 배터리 지속 시간 · 관측자 조건 · 섬광 쿨타임~~ | ❌ **과제 자체가 삭제됨 (§08 · §03 · §04)** |

> **1번이 새 병목이다.** v0.5의 1번은 에셋이었고, 그것도 여전히 리스크지만, 20인은
> 4인과 다른 종류의 문제다. 「4인 게임에서 성능 한계에 부딪힐 일은 없다」는 v0.5의
> 문장은 20인에서 참이 아니다. 스무 명이 마지막 관문 하나에 몰리는 순간이 **설계상
> 반드시 일어나야 하는 순간**이므로(§11), 그 순간이 가장 무거운 프레임이 된다.
> 관심 관리로 잘라낼 수 없는 유일한 장면이기도 하다 — 다 같은 방에 있으니까.

> **2번 리스크**
>
> **1인칭이어도 캐릭터 모델이 필요하다** — 앞에 사람이 있다는 사실이 정보다(§05).
> 괴물 1종 + 플레이어 1종 + 애니메이션. 직업 색이 없어져 **모델은 하나로 줄었다.**
> 프로토타입에서는 캡슐로 대체하고 **에셋스토어를 미리 조사해 둘 것.**

---

*20인 경쟁 하강 경주 · 설계 문서 v1.1 · 2026-08-03 · 16개 섹션*
